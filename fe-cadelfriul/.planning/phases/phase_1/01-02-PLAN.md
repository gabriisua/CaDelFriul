---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/separator.tsx
autonomous: true
requirements:
  - SCFLD-01
  - SCFLD-02
  - SCFLD-03

must_haves:
  truths:
    - "shadcn card component is available for RoomCard, ExperienceCard, ProductCard in (vetrina) pages"
    - "shadcn input and label components are available for form fields on (auth) pages"
    - "shadcn skeleton component is available for loading states across all route groups"
    - "shadcn separator component is available for dashboard sidebar sections"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter primitives"
      min_lines: 20
    - path: "src/components/ui/input.tsx"
      provides: "<input> with shadcn styling"
      min_lines: 15
    - path: "src/components/ui/label.tsx"
      provides: "<label> with shadcn styling"
      min_lines: 10
    - path: "src/components/ui/skeleton.tsx"
      provides: "<div> with skeleton animation class"
      min_lines: 5
    - path: "src/components/ui/separator.tsx"
      provides: "<div> with separator styling"
      min_lines: 5
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "@/lib/utils"
      via: "cn() import"
      pattern: "import.*cn.*from.*@/lib/utils"
    - from: "src/components/ui/input.tsx"
      to: "@/lib/utils"
      via: "cn() import"
      pattern: "import.*cn.*from.*@/lib/utils"
---

<objective>
Install all shadcn/ui components needed by Phase 1 route groups.

Purpose: Make card, input, label, skeleton, and separator primitives available for the vetrina (card-based pages), auth (form fields), and dashboard (sidebar separators, loading skeletons) route groups. These are foundational dependencies that route group plans import.

Output: Five new shadcn component files in src/components/ui/.
</objective>

<execution_context>
@/Users/gabrielesuardi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/gabrielesuardi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase_1/01-UI-SPEC.md (Component Inventory §lines 99-113)
@components.json

<interfaces>
shadcn config (from components.json, already read):
```json
{
  "style": "base-nova",
  "rsc": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "iconLibrary": "lucide",
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  }
}
```

Existing component (from button.tsx, already read): Uses `@base-ui/react/button` (base-nova preset), `cva` from `class-variance-authority`, `cn` from `@/lib/utils`.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn card, input, label, skeleton, and separator components</name>
  <files>
    src/components/ui/card.tsx
    src/components/ui/input.tsx
    src/components/ui/label.tsx
    src/components/ui/skeleton.tsx
    src/components/ui/separator.tsx
  </files>

  <read_first>
    components.json (verify base-nova preset and aliases — already read)
    src/components/ui/button.tsx (verify base-nova import pattern — already read: uses @base-ui/react/button)
  </read_first>

  <action>
    Run the shadcn CLI to add all five components in a single command:

    ```bash
    npx shadcn add card input label skeleton separator --yes
    ```

    The `--yes` flag auto-confirms installation for each.

    The CLI reads components.json, detects the base-nova preset, and generates each component file under `src/components/ui/` using `@base-ui/react/*` imports (same pattern as the existing button.tsx). All components will use `cn()` from `@/lib/utils`.

    After installation, verify that:
    1. Each file exists and exports the expected components
    2. All files use `@/lib/utils` for `cn()` imports
    3. No files use `@radix-ui/*` (base-nova preset must use `@base-ui/react/*`)

    If any component fails to install (e.g., not available in the base-nova preset), note the failure in the SUMMARY — but the component may still be available for import if the CLI generates a standalone version.
  </action>

  <verify>
    <automated>
      ls src/components/ui/card.tsx src/components/ui/input.tsx src/components/ui/label.tsx src/components/ui/skeleton.tsx src/components/ui/separator.tsx 2>/dev/null | wc -l
    </automated>
  </verify>

  <done>
    All 5 files exist in src/components/ui/. Each exports at least one component (e.g., Card, Input, Label, Skeleton, Separator).
  </done>

  <acceptance_criteria>
    1. `ls src/components/ui/card.tsx src/components/ui/input.tsx src/components/ui/label.tsx src/components/ui/skeleton.tsx src/components/ui/separator.tsx 2>/dev/null | wc -l` returns 5
    2. `grep -c "@/lib/utils" src/components/ui/card.tsx` returns >= 1 (uses cn())
    3. `grep -c "@/lib/utils" src/components/ui/input.tsx` returns >= 1
    4. `npm run build` or `npx next build` succeeds (all components compile correctly with the theme)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI install → filesystem | shadcn CLI writes component files to src/components/ui/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-003 | S | CLI install | accept | shadcn CLI is trusted first-party tool. Components are copied locally — no remote runtime dependencies. |
</threat_model>

<verification>
- All 5 component files exist
- `npm run dev` starts without import errors
- Components render without visual regressions (no styling conflicts with brand theme from Plan 01)
</verification>

<success_criteria>
1. card.tsx exports Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
2. input.tsx exports Input component
3. label.tsx exports Label component
4. skeleton.tsx exports Skeleton component
5. separator.tsx exports Separator component
6. All pass TypeScript compilation with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
