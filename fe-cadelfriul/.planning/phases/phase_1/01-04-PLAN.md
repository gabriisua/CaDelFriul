---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03-PLAN.md
files_modified:
  - src/app/(vetrina)/_components/ExperienceCard.tsx
  - src/app/(vetrina)/experiences/page.tsx
  - src/app/(vetrina)/_components/ProductCard.tsx
  - src/app/(vetrina)/products/page.tsx
  - src/app/(vetrina)/about/page.tsx
  - src/app/(vetrina)/contact/page.tsx
autonomous: true
requirements:
  - VETR-03
  - VETR-04
  - VETR-05
  - VETR-06

must_haves:
  truths:
    - "visitor can navigate to /experiences and see a grid of ExperienceCards with title, duration, description"
    - "visitor can navigate to /products and see a grid of ProductCards with name and price"
    - "visitor can read the resort's heritage story on /about"
    - "visitor can see contact details and location info on /contact"
    - "all (vetrina) pages share the sticky Header + Footer from the layout"
  artifacts:
    - path: "src/app/(vetrina)/_components/ExperienceCard.tsx"
      provides: "Experience card with image, title, duration, description"
      min_lines: 25
    - path: "src/app/(vetrina)/experiences/page.tsx"
      provides: "Experiences catalog page"
      contains: "Spa & Wellness"
    - path: "src/app/(vetrina)/_components/ProductCard.tsx"
      provides: "Product card with name and price"
      min_lines: 25
    - path: "src/app/(vetrina)/products/page.tsx"
      provides: "Products/merchandise page"
      contains: "Local Products"
    - path: "src/app/(vetrina)/about/page.tsx"
      provides: "Heritage storytelling page"
      min_lines: 40
    - path: "src/app/(vetrina)/contact/page.tsx"
      provides: "Contact and location page"
      contains: "Get in Touch"
  key_links:
    - from: "src/app/(vetrina)/experiences/page.tsx"
      to: "src/app/(vetrina)/_components/ExperienceCard.tsx"
      via: "import"
      pattern: "import ExperienceCard from"
    - from: "src/app/(vetrina)/products/page.tsx"
      to: "src/app/(vetrina)/_components/ProductCard.tsx"
      via: "import"
      pattern: "import ProductCard from"
    - from: "src/app/(vetrina)/"
      to: "src/app/(vetrina)/layout.tsx"
      via: "Next.js route group nesting"
      pattern: "layout wraps all pages"
---

<objective>
Build the remaining public showcase pages — Experiences, Products, About, and Contact — and their card components.

Purpose: Complete the public-facing showcase so visitors can browse all resort offerings (experiences, local products), learn the resort heritage, and find contact/location information. These pages share the (vetrina) Header + Footer layout created in Plan 03.

Output: Four new pages and three new components under the (vetrina) route group.
</objective>

<execution_context>
@/Users/gabrielesuardi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/gabrielesuardi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase_1/01-UI-SPEC.md (Layout Contracts §(vetrina) pages lines 143-148, Component Inventory lines 120-128)
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From shadcn card.tsx: `import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card"`
From src/app/(vetrina)/layout.tsx (created in Plan 03): wraps all pages in Header + Footer automatically
Theme tokens: bg-background (ivory), bg-secondary (stone), text-accent (gold), font-heading (Playfair), font-sans (Geist)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExperienceCard component and Experiences page</name>
  <files>
    src/app/(vetrina)/_components/ExperienceCard.tsx
    src/app/(vetrina)/experiences/page.tsx
  </files>

  <read_first>
    .planning/phases/phase_1/01-UI-SPEC.md (Component Inventory line 122 — ExperienceCard with image, title, duration, description)
    src/app/(vetrina)/_components/RoomCard.tsx (pattern reference — card component uses same Card import pattern)
  </read_first>

  <action>
    Create TWO files. Create the directory first: `mkdir -p src/app/\(vetrina\)/experiences`

    FILE 1: src/app/(vetrina)/_components/ExperienceCard.tsx
    Follow the same pattern as RoomCard but for experiences:
    - Import Card, CardContent, CardDescription, CardHeader, CardTitle from @/components/ui/card
    - Props: title (string), duration (string), description (string)
    - Decorative gold top border: `border-t-2 border-accent`
    - Duration shown as a small badge/tag
    - Description in muted text

    ```tsx
    import {
      Card,
      CardContent,
      CardDescription,
      CardHeader,
      CardTitle,
    } from "@/components/ui/card";

    interface ExperienceCardProps {
      title: string;
      duration: string;
      description: string;
    }

    export default function ExperienceCard({
      title,
      duration,
      description,
    }: ExperienceCardProps) {
      return (
        <Card className="border-t-2 border-accent pt-0">
          <CardHeader>
            <CardTitle className="font-heading text-xl font-semibold">
              {title}
            </CardTitle>
            <CardDescription className="text-sm">{description}</CardDescription>
          </CardHeader>
          <CardContent>
            <span className="inline-block rounded-full bg-secondary px-3 py-1 text-xs font-medium text-secondary-foreground">
              {duration}
            </span>
          </CardContent>
        </Card>
      );
    }
    ```

    FILE 2: src/app/(vetrina)/experiences/page.tsx
    - Import ExperienceCard from `../_components/ExperienceCard`
    - Static data with 6 experiences (Spa & Wellness, Wine Tasting, Cooking Class, etc.)
    - Page title: "Experiences & Activities" in Playfair Display
    - Grid: `grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3`
    - Same page structure as Rooms page

    ```tsx
    import ExperienceCard from "../_components/ExperienceCard";

    const experiences = [
      {
        title: "Spa & Wellness",
        duration: "2 hours",
        description:
          "Rejuvenate body and mind with our signature treatments using local Friulian ingredients — honey, grapeseed oil, and mountain herbs.",
      },
      {
        title: "Wine Tasting",
        duration: "1.5 hours",
        description:
          "Sample the finest Collio and Friuli wines guided by our resident sommelier, featuring vintages from nearby estates.",
      },
      {
        title: "Cooking Class",
        duration: "3 hours",
        description:
          "Learn to prepare authentic Friulian dishes — from frico to gnocchi di susine — with our chef in the resort kitchen.",
      },
      {
        title: "Olive Grove Tour",
        duration: "1 hour",
        description:
          "Stroll through our centuries-old olive groves and learn about traditional oil production, with a tasting of our own extra virgin olive oil.",
      },
      {
        title: "Truffle Hunting",
        duration: "4 hours",
        description:
          "Join a local trifolau and trained dogs in the woods of Friuli for an unforgettable truffle hunt, followed by a tasting.",
      },
      {
        title: "Cycling the Collio",
        duration: "Half day",
        description:
          "Explore the rolling hills of the Collio wine region on a guided e-bike tour, with stops at panoramic viewpoints and vineyards.",
      },
    ];

    export default function ExperiencesPage() {
      return (
        <div className="mx-auto max-w-7xl px-4 py-16 md:px-8">
          <h1 className="font-heading text-3xl font-semibold md:text-4xl">
            Experiences & Activities
          </h1>
          <p className="mt-3 text-muted-foreground">
            Immerse yourself in the culture, flavors, and landscapes of Friuli with our
            curated selection of experiences.
          </p>
          <div className="mt-10 grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
            {experiences.map((exp) => (
              <ExperienceCard key={exp.title} {...exp} />
            ))}
          </div>
        </div>
      );
    }
    ```
  </action>

  <verify>
    <automated>
      ls src/app/\(vetrina\)/_components/ExperienceCard.tsx && ls src/app/\(vetrina\)/experiences/page.tsx && grep -c "Spa & Wellness" src/app/\(vetrina\)/experiences/page.tsx && grep -c "border-accent" src/app/\(vetrina\)/_components/ExperienceCard.tsx
    </automated>
  </verify>

  <done>
    ExperienceCard renders with gold border, title, duration badge, description. Experiences page displays 6 cards in responsive grid. Page shares (vetrina) Header/Footer layout.
  </done>

  <acceptance_criteria>
    1. `ls src/app/(vetrina)/_components/ExperienceCard.tsx` succeeds
    2. `ls src/app/(vetrina)/experiences/page.tsx` succeeds
    3. `grep -c "border-accent" src/app/(vetrina)/_components/ExperienceCard.tsx` returns 1
    4. `grep -c "Experiences & Activities" src/app/(vetrina)/experiences/page.tsx` returns 1
    5. `grep -c "Spa & Wellness" src/app/(vetrina)/experiences/page.tsx` returns 1
    6. `grep -c "grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3" src/app/(vetrina)/experiences/page.tsx` returns 1
    7. `grep -c "import ExperienceCard from" src/app/(vetrina)/experiences/page.tsx` returns 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create ProductCard component and Products page</name>
  <files>
    src/app/(vetrina)/_components/ProductCard.tsx
    src/app/(vetrina)/products/page.tsx
  </files>

  <read_first>
    .planning/phases/phase_1/01-UI-SPEC.md (Component Inventory line 124 — ProductCard with image, name, price)
    src/app/(vetrina)/_components/RoomCard.tsx (pattern reference)
  </read_first>

  <action>
    Create TWO files. Create the directory first: `mkdir -p src/app/\(vetrina\)/products`

    FILE 1: src/app/(vetrina)/_components/ProductCard.tsx
    - Import Card, CardContent, CardDescription, CardHeader, CardTitle from @/components/ui/card
    - Props: name (string), price (string), description (string)
    - Decorative gold top border: `border-t-2 border-accent`
    - Price in accent color

    ```tsx
    import {
      Card,
      CardContent,
      CardDescription,
      CardHeader,
      CardTitle,
    } from "@/components/ui/card";

    interface ProductCardProps {
      name: string;
      price: string;
      description: string;
    }

    export default function ProductCard({
      name,
      price,
      description,
    }: ProductCardProps) {
      return (
        <Card className="border-t-2 border-accent pt-0">
          <CardHeader>
            <CardTitle className="font-heading text-xl font-semibold">
              {name}
            </CardTitle>
            <CardDescription className="text-sm">{description}</CardDescription>
          </CardHeader>
          <CardContent>
            <p className="text-2xl font-semibold text-accent">{price}</p>
          </CardContent>
        </Card>
      );
    }
    ```

    FILE 2: src/app/(vetrina)/products/page.tsx
    - Import ProductCard from `../_components/ProductCard`
    - Static data with 6 products (local wine, olive oil, gift baskets, etc.)
    - Page title: "Local Products" in Playfair Display
    - Same grid layout

    ```tsx
    import ProductCard from "../_components/ProductCard";

    const products = [
      {
        name: "Friulano DOC",
        price: "€28",
        description:
          "A crisp, elegant white wine from the Collio region. Notes of almond and white flowers.",
      },
      {
        name: "Extra Virgin Olive Oil",
        price: "€18",
        description:
          "Cold-pressed from our estate groves. Golden-green with a peppery finish.",
      },
      {
        name: "Gift Basket — Rustic",
        price: "€65",
        description:
          "A curated selection of local cheeses, cured meats, honey, and a bottle of Refosco.",
      },
      {
        name: "Saffron Threads",
        price: "€12",
        description:
          "Premium Friulian saffron, hand-harvested from the plains of San Daniele.",
      },
      {
        name: "Gift Basket — Premium",
        price: "€120",
        description:
          "Our finest selection: Friulano DOC, olive oil, truffle pâté, aged cheese, and grappa.",
      },
      {
        name: "Grappa di Picolit",
        price: "€45",
        description:
          "An aromatic grappa distilled from Picolit grapes. Smooth and floral.",
      },
    ];

    export default function ProductsPage() {
      return (
        <div className="mx-auto max-w-7xl px-4 py-16 md:px-8">
          <h1 className="font-heading text-3xl font-semibold md:text-4xl">
            Local Products
          </h1>
          <p className="mt-3 text-muted-foreground">
            Take a piece of Friuli home with you. Our selection of local wines, artisanal
            foods, and handcrafted gifts.
          </p>
          <div className="mt-10 grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
            {products.map((product) => (
              <ProductCard key={product.name} {...product} />
            ))}
          </div>
        </div>
      );
    }
    ```
  </action>

  <verify>
    <automated>
      ls src/app/\(vetrina\)/_components/ProductCard.tsx && ls src/app/\(vetrina\)/products/page.tsx && grep -c "Local Products" src/app/\(vetrina\)/products/page.tsx && grep -c "text-accent" src/app/\(vetrina\)/_components/ProductCard.tsx && grep -c "Friulano" src/app/\(vetrina\)/products/page.tsx
    </automated>
  </verify>

  <done>
    ProductCard renders with gold border, name, price in accent color. Products page displays 6 items in responsive grid.
  </done>

  <acceptance_criteria>
    1. `ls src/app/(vetrina)/_components/ProductCard.tsx` succeeds
    2. `ls src/app/(vetrina)/products/page.tsx` succeeds
    3. `grep -c "border-accent" src/app/(vetrina)/_components/ProductCard.tsx` returns 1
    4. `grep -c "text-accent" src/app/(vetrina)/_components/ProductCard.tsx` returns 1
    5. `grep -c "Local Products" src/app/(vetrina)/products/page.tsx` returns 1
    6. `grep -c "Friulano" src/app/(vetrina)/products/page.tsx` returns 1 (has real product data)
    7. `grep -c "grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3" src/app/(vetrina)/products/page.tsx` returns 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create About page and Contact page</name>
  <files>
    src/app/(vetrina)/about/page.tsx
    src/app/(vetrina)/contact/page.tsx
  </files>

  <read_first>
    .planning/phases/phase_1/01-UI-SPEC.md (Layout Contracts §VETR-05 About/heritage storytelling, §VETR-06 ContactMap; Component Inventory line 126 — ContactMap component)
  </read_first>

  <action>
    Create TWO files. Create the directories first: `mkdir -p src/app/\(vetrina\)/about src/app/\(vetrina\)/contact`

    FILE 1: src/app/(vetrina)/about/page.tsx
    Heritage storytelling page with:
    - Title: "Our Story" in Playfair Display font-heading
    - A narrative section about the resort's heritage (Friuli region, family tradition, the estate)
    - Two columns on desktop: text left, visual placeholder right
    - Values/mission section

    ```tsx
    export default function AboutPage() {
      return (
        <div className="mx-auto max-w-7xl px-4 py-16 md:px-8">
          <h1 className="font-heading text-3xl font-semibold md:text-4xl">
            Our Story
          </h1>

          <div className="mt-12 grid gap-12 md:grid-cols-2">
            <div>
              <h2 className="font-heading text-2xl font-semibold">
                A Legacy in the Heart of Friuli
              </h2>
              <p className="mt-4 leading-relaxed text-muted-foreground">
                Nestled among the rolling hills of the Collio wine region, Ca&apos; Del Friul
                has been a sanctuary of Italian hospitality for over a century. What began as a
                modest country estate has blossomed into one of Italy&apos;s most cherished resort
                destinations — a place where every stone tells a story and every meal celebrates
                the land.
              </p>
              <p className="mt-4 leading-relaxed text-muted-foreground">
                Our family has tended these grounds through five generations, preserving the
                traditions of Friulian craftsmanship, cuisine, and warmth. Today, we welcome
                guests from around the world to experience the simple, profound beauty of this
                corner of Italy.
              </p>
              <h2 className="mt-10 font-heading text-2xl font-semibold">
                Our Philosophy
              </h2>
              <p className="mt-4 leading-relaxed text-muted-foreground">
                We believe true luxury is found in authenticity — in the taste of a sun-ripened
                tomato, the scent of hay in a summer meadow, the warmth of a handshake from a
                local winemaker. Every experience at Ca&apos; Del Friul is designed to connect you
                with the soul of this land.
              </p>
            </div>
            <div className="flex items-center justify-center rounded-lg bg-secondary p-16 text-center">
              <p className="text-sm italic text-muted-foreground">
                [Visual placeholder — heritage imagery]
              </p>
            </div>
          </div>
        </div>
      );
    }
    ```

    FILE 2: src/app/(vetrina)/contact/page.tsx
    Contact & Location page with:
    - Title: "Get in Touch" in Playfair Display
    - Two columns: contact details left, map placeholder right
    - Contact details: address, phone, email, social links
    - A simple form placeholder (not functional in Phase 1 — just UI structure with inputs)
    - Use shadcn Input component from @/components/ui/input

    ```tsx
    import { Input } from "@/components/ui/input";
    import { Label } from "@/components/ui/label";
    import { Button } from "@/components/ui/button";

    export default function ContactPage() {
      return (
        <div className="mx-auto max-w-7xl px-4 py-16 md:px-8">
          <h1 className="font-heading text-3xl font-semibold md:text-4xl">
            Get in Touch
          </h1>
          <p className="mt-3 text-muted-foreground">
            We&apos;d love to hear from you. Reach out with questions, special requests, or to
            start planning your stay.
          </p>

          <div className="mt-12 grid gap-12 md:grid-cols-2">
            {/* Contact details */}
            <div>
              <h2 className="font-heading text-2xl font-semibold">Contact Details</h2>
              <dl className="mt-6 space-y-6">
                <div>
                  <dt className="text-sm font-medium text-foreground">Address</dt>
                  <dd className="mt-1 text-muted-foreground">
                    Via del Resort, 1<br />
                    33010 Friuli, Italy
                  </dd>
                </div>
                <div>
                  <dt className="text-sm font-medium text-foreground">Phone</dt>
                  <dd className="mt-1 text-muted-foreground">+39 0432 123 456</dd>
                </div>
                <div>
                  <dt className="text-sm font-medium text-foreground">Email</dt>
                  <dd className="mt-1 text-muted-foreground">info@cadelfriul.it</dd>
                </div>
              </dl>

              {/* Map placeholder */}
              <div className="mt-8 flex aspect-[4/3] items-center justify-center rounded-lg bg-secondary">
                <p className="text-sm italic text-muted-foreground">
                  [Map placeholder — embedded map in future phase]
                </p>
              </div>
            </div>

            {/* Contact form placeholder */}
            <div>
              <h2 className="font-heading text-2xl font-semibold">Send a Message</h2>
              <form className="mt-6 space-y-6" onSubmit={(e) => e.preventDefault()}>
                <div className="space-y-2">
                  <Label htmlFor="name">Name</Label>
                  <Input id="name" placeholder="Your name" required />
                </div>
                <div className="space-y-2">
                  <Label htmlFor="email">Email</Label>
                  <Input id="email" type="email" placeholder="your@email.com" required />
                </div>
                <div className="space-y-2">
                  <Label htmlFor="message">Message</Label>
                  <textarea
                    id="message"
                    rows={5}
                    className="flex w-full rounded-lg border border-input bg-background px-3 py-2 text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50"
                    placeholder="Your message..."
                    required
                  ></textarea>
                </div>
                <Button type="submit" variant="default">
                  Send Message
                </Button>
              </form>
            </div>
          </div>
        </div>
      );
    }
    ```

    NOTE: The textarea is a native HTML element (not shadcn) since shadcn doesn't provide a textarea component. It's styled to match the Input component's styling.

    ACCEPTANCE: The contact form's `onSubmit` handler prevents default behavior (no page reload) but does nothing else — it's a static placeholder per AUTH-01/02/03 spec ("static placeholder — no real validation in Phase 1").
  </action>

  <verify>
    <automated>
      ls src/app/\(vetrina\)/about/page.tsx && ls src/app/\(vetrina\)/contact/page.tsx && grep -c "Our Story" src/app/\(vetrina\)/about/page.tsx && grep -c "Get in Touch" src/app/\(vetrina\)/contact/page.tsx && grep -c "info@cadelfriul.it" src/app/\(vetrina\)/contact/page.tsx
    </automated>
  </verify>

  <done>
    About page tells the resort's heritage story with two-column layout. Contact page shows address, phone, email, map placeholder, and contact form with Input/Label components and Submit button.
  </done>

  <acceptance_criteria>
    1. `ls src/app/(vetrina)/about/page.tsx` succeeds
    2. `ls src/app/(vetrina)/contact/page.tsx` succeeds
    3. `grep -c "Our Story" src/app/(vetrina)/about/page.tsx` returns 1
    4. `grep -c "font-heading text-2xl" src/app/(vetrina)/about/page.tsx` returns 2 (two heading sections)
    5. `grep -c "Get in Touch" src/app/(vetrina)/contact/page.tsx` returns 1
    6. `grep -c "info@cadelfriul.it" src/app/(vetrina)/contact/page.tsx` returns 1
    7. `grep -c "@/components/ui/input" src/app/(vetrina)/contact/page.tsx` returns 1 (uses shadcn Input)
    8. `grep -c "@/components/ui/label" src/app/(vetrina)/contact/page.tsx` returns 1 (uses shadcn Label)
    9. `grep -c "@/components/ui/button" src/app/(vetrina)/contact/page.tsx` returns 1 (has Submit button)
    10. `grep -c "Map placeholder" src/app/(vetrina)/contact/page.tsx` returns 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → static pages | Public pages with hardcoded content, no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-006 | S | Contact form | accept | Static placeholder with preventDefault on submit. No data is sent anywhere. Real validation added in Phase 2. |
</threat_model>

<verification>
- `npm run dev` starts without errors
- Navigate to `/experiences` — 6 ExperienceCards in grid
- Navigate to `/products` — 6 ProductCards in grid
- Navigate to `/about` — heritage storytelling layout renders
- Navigate to `/contact` — contact details + map placeholder + form render
- Header sticky on scroll across all pages, Footer present at bottom
</verification>

<success_criteria>
1. All 4 remaining (vetrina) pages render with correct content
2. Each page has consistent max-w-7xl container, responsive padding
3. All page titles use Playfair Display (font-heading)
4. Contact form has Input/Label shadcn components and Submit button
5. Build passes with no TypeScript errors
6. All pages share the (vetrina) Header/Footer layout automatically
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
