---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements:
  - SCFLD-04
  - SCFLD-05

must_haves:
  truths:
    - "root layout displays 'Ca' Del Friul — Premium Italian Resort' as page title"
    - "all site elements use the luxury rustic-chic color palette (warm ivory bg, stone gray secondary, muted gold accent)"
    - "headings render in Playfair Display 600 weight at 24px (h2-h4) and 56px (h1)"
    - "body text renders in Geist Sans 16px 400 weight"
    - "page scrolls smoothly on anchor link clicks"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Brand color palette tokens and font-heading mapping"
      min_lines: 130
    - path: "src/app/layout.tsx"
      provides: "Playfair Display font loading and updated metadata"
      contains: "Playfair_Display"
  key_links:
    - from: "src/app/globals.css"
      to: "src/app/layout.tsx"
      via: "--font-playfair CSS variable set by font loader"
      pattern: "--font-playfair"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "@theme inline --font-heading: var(--font-playfair)"
      pattern: "--font-heading.*--font-playfair"
---

<objective>
Apply the luxury rustic-chic brand identity to the entire application.

Purpose: Replace the generic shadcn neutral defaults with Ca' Del Friul's warm ivory, stone gray, and muted gold palette. Add Playfair Display as the heading font. Update site metadata from "Create Next App" boilerplate to the resort's brand identity. Enable smooth scrolling for anchor links.

Output: Customized globals.css with brand tokens, layout.tsx with Playfair font and proper metadata.
</objective>

<execution_context>
@/Users/gabrielesuardi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/gabrielesuardi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase_1/01-UI-SPEC.md

<interfaces>
Current layout.tsx structure (already read):
```tsx
// src/app/layout.tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
```

Current globals.css structure (already read):
- `@theme inline` block with CSS variable mappings
- `:root` block with neutral oklch values
- `.dark` block (unchanged for Phase 1)
- `@layer base` with `* {@apply border-border}`, `body {@apply bg-background text-foreground}`, `html {@apply font-sans}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply luxury rustic-chic brand color palette to globals.css</name>
  <files>src/app/globals.css</files>

  <read_first>
    src/app/globals.css (current state — already read)
    .planning/phases/phase_1/01-UI-SPEC.md (Color section, lines 67-93 for exact oklch values)
  </read_first>

  <action>
    Replace the entire `:root` block's CSS variable values with the luxury rustic-chic palette from UI-SPEC §Color.

    EXACT changes to make:
    
    1. In the `@theme inline` block, change line 12 from:
       `--font-heading: var(--font-sans);`
       to:
       `--font-heading: var(--font-playfair);`
       This connects the Tailwind `font-heading` utility class to the Playfair CSS variable (loaded in Task 2).

    2. Replace the `:root` block values with these EXACT oklch values:
       ```
       :root {
         --background: oklch(0.97 0.01 80);
         --foreground: oklch(0.205 0.02 60);
         --card: oklch(0.97 0.01 80);
         --card-foreground: oklch(0.205 0.02 60);
         --popover: oklch(0.97 0.01 80);
         --popover-foreground: oklch(0.205 0.02 60);
         --primary: oklch(0.75 0.08 85);
         --primary-foreground: oklch(0.205 0.02 60);
         --secondary: oklch(0.87 0.01 70);
         --secondary-foreground: oklch(0.205 0.02 60);
         --muted: oklch(0.87 0.01 70);
         --muted-foreground: oklch(0.556 0 0);
         --accent: oklch(0.75 0.08 85);
         --accent-foreground: oklch(0.205 0.02 60);
         --destructive: oklch(0.577 0.245 27.325);
         --border: oklch(0.922 0 0);
         --input: oklch(0.922 0 0);
         --ring: oklch(0.708 0 0);
         --chart-1: oklch(0.87 0 0);
         --chart-2: oklch(0.556 0 0);
         --chart-3: oklch(0.439 0 0);
         --chart-4: oklch(0.371 0 0);
         --chart-5: oklch(0.269 0 0);
         --radius: 0.625rem;
         --sidebar: oklch(0.87 0.01 70);
         --sidebar-foreground: oklch(0.205 0.02 60);
         --sidebar-primary: oklch(0.75 0.08 85);
         --sidebar-primary-foreground: oklch(0.205 0.02 60);
         --sidebar-accent: oklch(0.87 0.01 70);
         --sidebar-accent-foreground: oklch(0.205 0.02 60);
         --sidebar-border: oklch(0.922 0 0);
         --sidebar-ring: oklch(0.708 0 0);
       }
       ```

    3. In the `@layer base` block, add `scroll-behavior: smooth;` to the `html` rule (currently line 128-130):
       Change from:
       ```css
       html {
         @apply font-sans;
       }
       ```
       to:
       ```css
       html {
         @apply font-sans;
         scroll-behavior: smooth;
       }
       ```

    IMPORTANT: Do NOT modify the `.dark` block. Phase 1 does not implement dark mode per UI-SPEC §Color (line 93: "do not customize for Phase 1").

    Import notes: The file already has `@import "tailwindcss"`, `@import "tw-animate-css"`, `@import "shadcn/tailwind.css"` at the top — leave these untouched.
  </action>

  <verify>
    <automated>
      grep -c "oklch(0.97 0.01 80)" src/app/globals.css && grep -c "oklch(0.75 0.08 85)" src/app/globals.css && grep -c "oklch(0.87 0.01 70)" src/app/globals.css && grep -c "scroll-behavior: smooth" src/app/globals.css && grep -c "font-playfair" src/app/globals.css
    </automated>
  </verify>

  <done>
    All four greps return >= 1. The `:root` block uses warm ivory backgrounds, stone gray secondary/sidebar, muted gold primary/accent. scroll-behavior:smooth is present. `@theme inline` maps `--font-heading` to `var(--font-playfair)`.
  </done>

  <acceptance_criteria>
    1. `grep -c "oklch(0.97 0.01 80)" src/app/globals.css` returns at least 3 (background, card, popover all use warm ivory)
    2. `grep -c "oklch(0.75 0.08 85)" src/app/globals.css` returns at least 2 (primary, accent use muted gold)
    3. `grep -c "oklch(0.87 0.01 70)" src/app/globals.css` returns at least 2 (secondary, muted, sidebar use stone gray)
    4. `grep -c "scroll-behavior: smooth" src/app/globals.css` returns 1
    5. `grep -c "font-playfair" src/app/globals.css` returns 1
    6. `.dark` block values are UNCHANGED from shadcn neutral defaults:
       - `grep -c "oklch(0.145 0 0)" src/app/globals.css` returns 5 (background, foreground, card, popover in .dark)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Playfair Display font and update root metadata in layout.tsx</name>
  <files>src/app/layout.tsx</files>

  <read_first>
    src/app/layout.tsx (current state — already read)
    node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md (Using Multiple Fonts → CSS Variables method)
    .planning/phases/phase_1/01-UI-SPEC.md (Typography §lines 50-64, Copywriting Contract §lines 213-224)
  </read_first>

  <action>
    Update src/app/layout.tsx with three changes:

    CHANGE 1 — Add Playfair Display font import and instance:
    ```tsx
    import { Geist, Geist_Mono, Playfair_Display } from "next/font/google";

    const geistSans = Geist({
      variable: "--font-geist-sans",
      subsets: ["latin"],
    });

    const geistMono = Geist_Mono({
      variable: "--font-geist-mono",
      subsets: ["latin"],
    });

    const playfairDisplay = Playfair_Display({
      variable: "--font-playfair",
      subsets: ["latin"],
      weight: ["600"],
      display: "swap",
    });
    ```

    CHANGE 2 — Update the exported metadata object:
    ```tsx
    export const metadata: Metadata = {
      title: "Ca' Del Friul — Premium Italian Resort",
      description:
        "Discover the heart of Friuli. Experience timeless elegance at Ca' Del Friul — a premium Italian resort offering luxury rooms, authentic experiences, and local products.",
    };
    ```
    Use the EXACT strings from UI-SPEC §Copywriting Contract.

    CHANGE 3 — Add playfairDisplay.variable to the `<html>` className:
    ```tsx
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} ${playfairDisplay.variable} h-full antialiased`}
    >
    ```

    Keep all other existing code unchanged: the Geist font configs, the `body` tag, and the `children` prop typing.
    The lang should remain "en" (i18n deferred to Phase 5).
  </action>

  <verify>
    <automated>
      grep -c "Playfair_Display" src/app/layout.tsx && grep -c "Ca' Del Friul" src/app/layout.tsx && grep -c "playfairDisplay" src/app/layout.tsx
    </automated>
  </verify>

  <done>
    Layout loads Playfair Display with --font-playfair variable. Metadata shows "Ca' Del Friul — Premium Italian Resort" as title and the full description from UI-SPEC. The html className includes playfairDisplay.variable.
  </done>

  <acceptance_criteria>
    1. `grep -c "Playfair_Display" src/app/layout.tsx` returns >= 1 (import statement)
    2. `grep -c "--font-playfair" src/app/layout.tsx` returns 1 (variable option in font config)
    3. `grep -c "playfairDisplay" src/app/layout.tsx` returns 2 (font instance + className usage)
    4. `grep -c "Ca' Del Friul — Premium Italian Resort" src/app/layout.tsx` returns 1 (metadata title)
    5. `grep "Discover the heart of Friuli" src/app/layout.tsx` returns 0 lines matching the description
    6. `npm run build` or `npx next build` succeeds with no errors (layout compiles)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root layout → browser | HTML head metadata rendered server-side, consumed by browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-001 | T | Metadata string | accept | Static strings only, no user input. No injection surface. |
| T-01-002 | I | Font URL resolve | accept | next/font self-hosts fonts at build time — no runtime network requests. |
</threat_model>

<verification>
- `npm run dev` starts without errors
- `curl http://localhost:3000` returns HTML with `<title>Ca' Del Friul — Premium Italian Resort</title>`
- Page has no visible "Create Next App" text or brand references
</verification>

<success_criteria>
1. Root layout metadata updated — no "Create Next App" anywhere in output
2. All pages use the warm ivory/stone gray/muted gold palette
3. Playfair Display renders headings via `font-heading` utility class
4. Geist Sans renders body text via `font-sans` utility class
5. `scroll-behavior: smooth` enables smooth anchor scrolling
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
