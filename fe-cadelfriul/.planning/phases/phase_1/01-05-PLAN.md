---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01-PLAN.md
  - 01-02-PLAN.md
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/_components/AuthLayout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/(auth)/reset-password/page.tsx
  - src/app/(auth)/loading.tsx
  - src/app/(auth)/error.tsx
  - src/app/(auth)/not-found.tsx
autonomous: true
requirements:
  - SCFLD-02
  - SCFLD-06
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "visitor can navigate to /login and see a centered form with email and password fields"
    - "visitor can navigate to /register and see a form with name, email, password, and confirm fields"
    - "visitor can navigate to /reset-password and see a form with email field only"
    - "all auth pages render without the public Header/Footer — clean, distraction-free layout"
    - "auth loading state shows a centered minimal skeleton"
    - "auth error state shows error message without header/footer chrome"
    - "auth 404 shows clean not-found without header/footer chrome"
  artifacts:
    - path: "src/app/(auth)/layout.tsx"
      provides: "Route group layout wrapping AuthLayout"
      min_lines: 5
    - path: "src/app/(auth)/_components/AuthLayout.tsx"
      provides: "Centered card layout with logo"
      min_lines: 30
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login form with email + password fields"
      contains: "Sign In"
    - path: "src/app/(auth)/register/page.tsx"
      provides: "Registration form with name + email + password + confirm fields"
      contains: "Create Account"
    - path: "src/app/(auth)/reset-password/page.tsx"
      provides: "Password reset form with email field"
      contains: "Reset Password"
    - path: "src/app/(auth)/loading.tsx"
      provides: "Minimal centered loading skeleton"
      min_lines: 5
    - path: "src/app/(auth)/error.tsx"
      provides: "Error state with clean centered layout"
      contains: "Something went wrong"
    - path: "src/app/(auth)/not-found.tsx"
      provides: "404 with clean centered layout"
      contains: "Page Not Found"
  key_links:
    - from: "src/app/(auth)/layout.tsx"
      to: "src/app/(auth)/_components/AuthLayout.tsx"
      via: "import"
      pattern: "import AuthLayout from"
    - from: "src/app/(auth)/login/page.tsx"
      to: "@/components/ui/input"
      via: "import Input"
      pattern: "import.*Input.*from.*@/components/ui/input"
    - from: "src/app/(auth)/login/page.tsx"
      to: "@/components/ui/label"
      via: "import Label"
      pattern: "import.*Label.*from.*@/components/ui/label"
---

<objective>
Build the auth route group with clean, distraction-free login, registration, and password-reset pages.

Purpose: Provide authentic-looking placeholder pages for the authentication flow. These pages render without the public Header/Footer — just a centered card with form fields, logo, and brand mark. Real auth logic comes in Phase 2.

Output: (auth) route group with layout, AuthLayout component, 3 auth pages, and per-group error/loading/not-found boundaries.
</objective>

<execution_context>
@/Users/gabrielesuardi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/gabrielesuardi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase_1/01-UI-SPEC.md (Layout Contracts §(auth) lines 165-185, Form fields Interaction §lines 253-261, Copywriting for metadata)
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From shadcn: `import { Input } from "@/components/ui/input"` (bordered, rounded), `import { Label } from "@/components/ui/label"` (14px Geist, weight 400), `import { Button } from "@/components/ui/button"`
From root layout (Plan 01): root layout wraps all routes with <html>/<body> — (auth) route group layout provides the inner content wrapper.
Theme background: `bg-background` (warm ivory) — even on auth pages.

AuthLayout spec (UI-SPEC §lines 180-185):
- No Header or Footer
- Centered vertically + horizontally, `max-w-md` card
- Logo or resort emblem above form
- "Ca' Del Friul" subtle brand mark

Form fields spec (UI-SPEC §lines 253-261):
- Input above label, 14px Geist, weight 400
- Submit button: primary variant, full-width
- HTML5 `required` only — no real validation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthLayout component and (auth) route group layout</name>
  <files>
    src/app/(auth)/layout.tsx
    src/app/(auth)/_components/AuthLayout.tsx
  </files>

  <read_first>
    .planning/phases/phase_1/01-UI-SPEC.md (Layout Contracts §(auth) lines 165-178, Auth layout spec lines 180-185)
    .planning/codebase/ARCHITECTURE.md (Layout nesting — root layout wraps route group layouts)
  </read_first>

  <action>
    Create TWO files. Create directories first: `mkdir -p src/app/\(auth\)/_components`

    FILE 1: src/app/(auth)/_components/AuthLayout.tsx
    - Wraps children in a centered layout
    - Full viewport vertical+horizontal centering: `min-h-screen flex items-center justify-center`
    - Logo text "Ca' Del Friul" in Playfair Display (font-heading)
    - Subtle brand mark: smaller text "Premium Italian Resort"
    - max-w-md card with padding, rounded corners, border
    - Background: `bg-background` (warm ivory)

    ```tsx
    export default function AuthLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <div className="flex min-h-screen items-center justify-center bg-background px-4">
          <div className="w-full max-w-md rounded-lg border border-border bg-card p-8 shadow-sm">
            <div className="mb-8 text-center">
              <h1 className="font-heading text-2xl font-semibold tracking-tight">
                Ca&apos; Del Friul
              </h1>
              <p className="mt-1 text-sm text-muted-foreground">
                Premium Italian Resort
              </p>
            </div>
            {children}
          </div>
        </div>
      );
    }
    ```

    FILE 2: src/app/(auth)/layout.tsx
    Route group layout that wraps pages with AuthLayout component:

    ```tsx
    import AuthLayout from "./_components/AuthLayout";

    export default function AuthRouteLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return <AuthLayout>{children}</AuthLayout>;
    }
    ```

    IMPORTANT: No "use client" needed in any file. Both are Server Components.
  </action>

  <verify>
    <automated>
      ls src/app/\(auth\)/layout.tsx && ls src/app/\(auth\)/_components/AuthLayout.tsx && grep -c "max-w-md" src/app/\(auth\)/_components/AuthLayout.tsx && grep -c "font-heading" src/app/\(auth\)/_components/AuthLayout.tsx && grep -c "Ca'" src/app/\(auth\)/_components/AuthLayout.tsx
    </automated>
  </verify>

  <done>
    AuthLayout provides centered max-w-md card layout with resort logo and brand mark. Route group layout wraps all auth pages with AuthLayout.
  </done>

  <acceptance_criteria>
    1. `ls src/app/(auth)/layout.tsx` succeeds
    2. `ls src/app/(auth)/_components/AuthLayout.tsx` succeeds
    3. `grep -c "max-w-md" src/app/(auth)/_components/AuthLayout.tsx` returns 1
    4. `grep -c "min-h-screen" src/app/(auth)/_components/AuthLayout.tsx` returns 1 (full viewport centering)
    5. `grep -c "font-heading" src/app/(auth)/_components/AuthLayout.tsx` returns 1 (Playfair Display logo)
    6. `grep -c "Premium Italian Resort" src/app/(auth)/_components/AuthLayout.tsx` returns 1
    7. `grep -c "import AuthLayout from" src/app/(auth)/layout.tsx` returns 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Login, Register, and Reset-password pages</name>
  <files>
    src/app/(auth)/login/page.tsx
    src/app/(auth)/register/page.tsx
    src/app/(auth)/reset-password/page.tsx
  </files>

  <read_first>
    .planning/phases/phase_1/01-UI-SPEC.md (Form fields Interaction §lines 253-261, AUTH-01/02/03 requirements, Component Inventory lines 108-109 — Input/Label usage)
    Task 1 of this plan (AuthLayout structure)
  </read_first>

  <action>
    Create THREE files. Create the directories first: `mkdir -p src/app/\(auth\)/login src/app/\(auth\)/register src/app/\(auth\)/reset-password`

    FILE 1: src/app/(auth)/login/page.tsx
    - Title within form: "Sign In" (h2, font-heading)
    - Email field with Label "Email" + Input (type="email", required, placeholder "your@email.com")
    - Password field with Label "Password" + Input (type="password", required, placeholder "Enter your password")
    - Submit button: "Sign In" — full width, variant="default"
    - "Don't have an account?" link to /register
    - "Forgot password?" link to /reset-password
    - Form uses `onSubmit={(e) => e.preventDefault()}` — static placeholder only

    ```tsx
    import { Button } from "@/components/ui/button";
    import { Input } from "@/components/ui/input";
    import { Label } from "@/components/ui/label";
    import Link from "next/link";

    export default function LoginPage() {
      return (
        <>
          <h2 className="mb-6 text-center font-heading text-xl font-semibold">
            Sign In
          </h2>
          <form onSubmit={(e) => e.preventDefault()} className="space-y-5">
            <div className="space-y-2">
              <Label htmlFor="email">Email</Label>
              <Input id="email" type="email" placeholder="your@email.com" required />
            </div>
            <div className="space-y-2">
              <Label htmlFor="password">Password</Label>
              <Input
                id="password"
                type="password"
                placeholder="Enter your password"
                required
              />
            </div>
            <Button type="submit" variant="default" className="w-full">
              Sign In
            </Button>
          </form>
          <div className="mt-6 text-center text-sm text-muted-foreground">
            <Link
              href="/reset-password"
              className="block hover:text-accent"
            >
              Forgot password?
            </Link>
            <p className="mt-2">
              Don&apos;t have an account?{" "}
              <Link href="/register" className="font-medium text-accent hover:underline">
                Sign up
              </Link>
            </p>
          </div>
        </>
      );
    }
    ```

    FILE 2: src/app/(auth)/register/page.tsx
    - Title: "Create Account"
    - Fields: Name (text, required), Email (email, required), Password (password, required, minLength=8), Confirm Password (password, required)
    - Submit: "Create Account" — full width, variant="default"
    - "Already have an account?" link to /login
    - Form is static placeholder only

    ```tsx
    import { Button } from "@/components/ui/button";
    import { Input } from "@/components/ui/input";
    import { Label } from "@/components/ui/label";
    import Link from "next/link";

    export default function RegisterPage() {
      return (
        <>
          <h2 className="mb-6 text-center font-heading text-xl font-semibold">
            Create Account
          </h2>
          <form onSubmit={(e) => e.preventDefault()} className="space-y-5">
            <div className="space-y-2">
              <Label htmlFor="name">Full Name</Label>
              <Input id="name" placeholder="John Doe" required />
            </div>
            <div className="space-y-2">
              <Label htmlFor="email">Email</Label>
              <Input id="email" type="email" placeholder="your@email.com" required />
            </div>
            <div className="space-y-2">
              <Label htmlFor="password">Password</Label>
              <Input
                id="password"
                type="password"
                placeholder="Create a password"
                required
                minLength={8}
              />
            </div>
            <div className="space-y-2">
              <Label htmlFor="confirm-password">Confirm Password</Label>
              <Input
                id="confirm-password"
                type="password"
                placeholder="Repeat your password"
                required
              />
            </div>
            <Button type="submit" variant="default" className="w-full">
              Create Account
            </Button>
          </form>
          <p className="mt-6 text-center text-sm text-muted-foreground">
            Already have an account?{" "}
            <Link href="/login" className="font-medium text-accent hover:underline">
              Sign in
            </Link>
          </p>
        </>
      );
    }
    ```

    FILE 3: src/app/(auth)/reset-password/page.tsx
    - Title: "Reset Password"
    - Instruction text: "Enter your email address and we'll send you a link to reset your password."
    - Email field only
    - Submit: "Send Reset Link" — full width, variant="default"
    - "Back to Sign In" link
    - Form is static placeholder

    ```tsx
    import { Button } from "@/components/ui/button";
    import { Input } from "@/components/ui/input";
    import { Label } from "@/components/ui/label";
    import Link from "next/link";

    export default function ResetPasswordPage() {
      return (
        <>
          <h2 className="mb-2 text-center font-heading text-xl font-semibold">
            Reset Password
          </h2>
          <p className="mb-6 text-center text-sm text-muted-foreground">
            Enter your email address and we&apos;ll send you a link to reset your
            password.
          </p>
          <form onSubmit={(e) => e.preventDefault()} className="space-y-5">
            <div className="space-y-2">
              <Label htmlFor="email">Email</Label>
              <Input id="email" type="email" placeholder="your@email.com" required />
            </div>
            <Button type="submit" variant="default" className="w-full">
              Send Reset Link
            </Button>
          </form>
          <p className="mt-6 text-center text-sm text-muted-foreground">
            <Link href="/login" className="font-medium text-accent hover:underline">
              Back to Sign In
            </Link>
          </p>
        </>
      );
    }
    ```

    All three forms:
    - Use `"use client"` NOT needed — the form `onSubmit` prevents default, but since these are static placeholders with no state or effects, they can be Server Components. Actually wait, the `onSubmit` handler is a JSX event handler, which requires client-side interactivity. So YES, all three need `"use client"` directive at the top.

    Add `"use client";` as the first line in each file.
  </action>

  <verify>
    <automated>
      ls src/app/\(auth\)/login/page.tsx && ls src/app/\(auth\)/register/page.tsx && ls src/app/\(auth\)/reset-password/page.tsx && grep -c "Sign In" src/app/\(auth\)/login/page.tsx && grep -c "Create Account" src/app/\(auth\)/register/page.tsx && grep -c "Reset Password" src/app/\(auth\)/reset-password/page.tsx && grep -c "use client" src/app/\(auth\)/login/page.tsx
    </automated>
  </verify>

  <done>
    Three auth pages created with proper form fields. Login has email+password. Register has name+email+password+confirm. Reset-password has email only. Each has submit button, navigation links, and uses shadcn Input/Label components. All have preventDefault form handlers (static placeholder).
  </done>

  <acceptance_criteria>
    1. `ls src/app/(auth)/login/page.tsx` succeeds
    2. `ls src/app/(auth)/register/page.tsx` succeeds
    3. `ls src/app/(auth)/reset-password/page.tsx` succeeds
    4. `grep -c "use client" src/app/(auth)/login/page.tsx` returns 1
    5. `grep -c "use client" src/app/(auth)/register/page.tsx` returns 1
    6. `grep -c "use client" src/app/(auth)/reset-password/page.tsx` returns 1
    7. `grep -c "Sign In" src/app/(auth)/login/page.tsx` returns 1
    8. `grep -c "Sign In" src/app/(auth)/login/page.tsx` returns 1
    9. `grep -c "Create Account" src/app/(auth)/register/page.tsx` returns 1
    10. `grep -c "Reset Password" src/app/(auth)/reset-password/page.tsx` returns 1
    11. `grep -c "@/components/ui/input" src/app/(auth)/login/page.tsx` returns 1
    12. `grep -c "@/components/ui/label" src/app/(auth)/login/page.tsx` returns 1
    13. `grep -c "@/components/ui/button" src/app/(auth)/login/page.tsx` returns 1
    14. `grep -c "preventDefault" src/app/(auth)/login/page.tsx` returns 1
    15. `grep -c "href=\"/register\"" src/app/(auth)/login/page.tsx` returns 1 (navigation link)
    16. `grep -c "href=\"/login\"" src/app/(auth)/register/page.tsx` returns 1
    17. `grep -c "href=\"/login\"" src/app/(auth)/reset-password/page.tsx` returns 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create loading, error, and not-found boundaries for (auth)</name>
  <files>
    src/app/(auth)/loading.tsx
    src/app/(auth)/error.tsx
    src/app/(auth)/not-found.tsx
  </files>

  <read_first>
    .planning/phases/phase_1/01-UI-SPEC.md (Layout Contracts §(auth) files lines 175-178 — loading.tsx "minimal skeleton", error.tsx "clean error state (no header/footer)", not-found.tsx "clean 404 (no header/footer)"; Copywriting §lines 218-222)
  </read_first>

  <action>
    Create THREE files — these render WITHIN the (auth) AuthLayout wrapper.

    FILE 1: src/app/(auth)/loading.tsx
    Minimal centered skeleton:

    ```tsx
    import { Skeleton } from "@/components/ui/skeleton";

    export default function AuthLoading() {
      return (
        <div className="space-y-5">
          <Skeleton className="mx-auto h-6 w-24" />
          <Skeleton className="h-10 w-full" />
          <Skeleton className="h-10 w-full" />
          <Skeleton className="h-10 w-full" />
        </div>
      );
    }
    ```

    FILE 2: src/app/(auth)/error.tsx
    - Must be a client component ("use client")
    - Same clean centered layout as AuthLayout but inline (no re-importing AuthLayout — it wraps this via layout)

    ```tsx
    "use client";

    import Link from "next/link";
    import { Button } from "@/components/ui/button";

    export default function AuthError({
      error,
      reset,
    }: {
      error: Error & { digest?: string };
      reset: () => void;
    }) {
      return (
        <div className="text-center">
          <h2 className="font-heading text-xl font-semibold">
            Something went wrong
          </h2>
          <p className="mt-2 text-sm text-muted-foreground">
            Please try refreshing the page. If the problem persists, contact us.
          </p>
          <div className="mt-6">
            <Button variant="default" asChild>
              <Link href="/">Return Home</Link>
            </Button>
          </div>
        </div>
      );
    }
    ```

    FILE 3: src/app/(auth)/not-found.tsx
    - No "use client" needed

    ```tsx
    import Link from "next/link";
    import { Button } from "@/components/ui/button";

    export default function AuthNotFound() {
      return (
        <div className="text-center">
          <h2 className="font-heading text-xl font-semibold">
            Page Not Found
          </h2>
          <p className="mt-2 text-sm text-muted-foreground">
            The page you&apos;re looking for doesn&apos;t exist. It may have been
            moved or deleted.
          </p>
          <div className="mt-6">
            <Button variant="default" asChild>
              <Link href="/">Return Home</Link>
            </Button>
          </div>
        </div>
      );
    }
    ```

    All use exact copy from UI-SPEC §Copywriting.
  </action>

  <verify>
    <automated>
      ls src/app/\(auth\)/loading.tsx && ls src/app/\(auth\)/error.tsx && ls src/app/\(auth\)/not-found.tsx && grep -c "Something went wrong" src/app/\(auth\)/error.tsx && grep -c "Page Not Found" src/app/\(auth\)/not-found.tsx && grep -c "use client" src/app/\(auth\)/error.tsx
    </automated>
  </verify>

  <done>
    Auth loading skeleton, error state, and 404 page created — all rendering within the clean AuthLayout wrapper (no Header/Footer).
  </done>

  <acceptance_criteria>
    1. `ls src/app/(auth)/loading.tsx` succeeds
    2. `ls src/app/(auth)/error.tsx` succeeds
    3. `ls src/app/(auth)/not-found.tsx` succeeds
    4. `grep -c "use client" src/app/(auth)/error.tsx` returns 1
    5. `grep -c "Something went wrong" src/app/(auth)/error.tsx` returns 1
    6. `grep -c "Page Not Found" src/app/(auth)/not-found.tsx` returns 1
    7. `grep -c "Return Home" src/app/(auth)/not-found.tsx` returns 1
    8. `grep -c "@/components/ui/skeleton" src/app/(auth)/loading.tsx` returns 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → (auth) pages | Public pages with placeholders, no real auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-007 | S | Auth forms (all) | accept | Static placeholders with preventDefault. No data submitted, no real validation. Real auth security added in Phase 2. |
| T-01-008 | T | Navigation links | accept | Static hardcoded routes (/login, /register, /reset-password). No user-controlled input in URLs. |
</threat_model>

<verification>
- `npm run dev` starts without errors
- Visit `/login` — centered card with email/password fields + "Sign In" button
- Visit `/register` — centered card with name/email/password/confirm fields + "Create Account" button
- Visit `/reset-password` — centered card with email field + "Send Reset Link" button
- Navigate between auth pages via links — all work
- Visit `/login/nonexistent` — auth not-found renders clean 404
- No Header or Footer visible on any auth page
</verification>

<success_criteria>
1. Login page at `/login` renders Sign In form
2. Register page at `/register` renders Create Account form
3. Reset-password page at `/reset-password` renders Reset Password form
4. All auth pages use clean centered AuthLayout no Header/Footer chrome
5. AuthLayout shows "Ca' Del Friul" logo and "Premium Italian Resort" brand mark
6. Navigation links between auth pages work
7. Error + loading + not-found boundaries render in clean layout
8. Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
