---
phase: 260727-k36
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(vetrina)/checkout/success/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Cart is cleared when success page loads"
    - "User sees a thank-you confirmation message"
    - "User can navigate to dashboard orders via a button"
    - "Page auto-redirects to orders after 6 seconds"
  artifacts:
    - path: "src/app/(vetrina)/checkout/success/page.tsx"
      provides: "Post-payment success page with cart clearing, thank-you UI, and navigation"
  key_links:
    - from: "src/app/(vetrina)/checkout/success/page.tsx"
      to: "src/context/CartContext.tsx"
      via: "useCart().clearCart() in useEffect"
      pattern: "clearCart"
    - from: "src/app/(vetrina)/checkout/success/page.tsx"
      to: "/dashboard/orders"
      via: "Link component"
      pattern: "dashboard/orders"
---

<objective>
Create a checkout success page at `/checkout/success` that Stripe redirects to after payment. The page clears the cart, shows a branded thank-you message, and provides navigation to the user's orders.

Purpose: Completes the payment flow — without this page, Stripe redirects into a void after checkout.
Output: New page component at `src/app/(vetrina)/checkout/success/page.tsx`
</objective>

<execution_context>
@/Users/gabrielesuardi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/gabrielesuardi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/context/CartContext.tsx:
```typescript
export function useCart(): CartContextValue;
// CartContextValue has: clearCart(): void, cartTotal: number, itemCount: number
```

From src/app/(vetrina)/layout.tsx:
```typescript
// VetrinaLayout wraps children with Header + Footer — success page inherits this automatically
```

From src/app/(vetrina)/checkout/page.tsx (pattern reference):
```typescript
// Existing checkout uses: buttonVariants, cn(), lucide icons, font-heading class
// Import pattern: import { Button, buttonVariants } from "@/components/ui/button";
// Link pattern: <Link href="/path" className={cn(buttonVariants({ variant: "default" }), "...")}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create checkout success page with cart clearing, thank-you UI, and auto-redirect</name>
  <files>src/app/(vetrina)/checkout/success/page.tsx, src/app/(vetrina)/checkout/success/page.test.tsx</files>
  <behavior>
    - Test 1: Page renders "Grazie" or "Thank you" heading and a confirmation message
    - Test 2: clearCart is called on mount (useEffect)
    - Test 3: "Gestisci i tuoi ordini" button/link points to /dashboard/orders
    - Test 4: Page auto-redirects to /dashboard/orders after approximately 6 seconds (via setTimeout in useEffect)
  </behavior>
  <action>
Create `src/app/(vetrina)/checkout/success/page.tsx` as a `"use client"` component. This page inherits `(vetrina)` layout (Header + Footer) automatically.

Implementation details:
1. Import `useCart` from `@/context/CartContext`, `useEffect`/`useState`/`useCallback` from React, `useRouter` from `next/navigation`, `Link` from `next/link`, `Button`/`buttonVariants` from `@/components/ui/button`, `cn` from `@/lib/utils`, `CheckCircle` from `lucide-react`.
2. On mount, call `clearCart()` from `useCart()` inside a `useEffect` with `[]` deps. Use a ref guard (`clearedRef`) to prevent double-clear in strict mode.
3. Read `session_id` from `useSearchParams()` for future order confirmation use — store in state but don't use yet (just log it). Wrap in a `<Suspense>` boundary since `useSearchParams()` requires it in Next.js 16.
4. Set up a 6-second auto-redirect: `useEffect` with a `setTimeout` that calls `router.push("/dashboard/orders")`. Clear timeout on unmount. Use `useState` for countdown display (optional: show "Redirecting in Xs…").
5. Render a centered layout matching the checkout empty-cart style: `max-w-7xl mx-auto px-4 py-24 md:px-8`, centered flex column. Green `CheckCircle` icon (size 16, `text-emerald-600`), heading "Grazie per il tuo ordine!", subtext "Il tuo ordine è stato confermato. Riceverai una email di conferma a breve.", then a "Gestisci i tuoi ordini" Link styled with `buttonVariants({ variant: "default", size: "lg" })` pointing to `/dashboard/orders`, and an optional "Continua lo shopping" secondary link to `/shop`.
6. For the `useSearchParams()` Suspense boundary: export a default wrapper component that renders `<Suspense fallback={...}><SuccessPageContent /></Suspense>`, with the actual content in a named `SuccessPageContent` component.

Create test file `src/app/(vetrina)/checkout/success/page.test.tsx` with the four behaviors above using React Testing Library and vitest/jest. Mock `useCart` and `next/navigation`.
  </action>
  <verify>
    <automated>npx vitest run src/app/\(vetrina\)/checkout/success/page.test.tsx 2>/dev/null || npx jest src/app/\(vetrina\)/checkout/success/page.test.tsx 2>/dev/null || echo "NO_TEST_RUNNER — verify with: npx tsc --noEmit"</automated>
  </verify>
  <done>
    - `src/app/(vetrina)/checkout/success/page.tsx` exists as a client component
    - `clearCart()` is called in a useEffect on mount
    - Page shows thank-you heading, confirmation text, and CheckCircle icon
    - "Gestisci i tuoi ordini" Link points to `/dashboard/orders`
    - Auto-redirect to `/dashboard/orders` after 6 seconds with timeout cleanup
    - `useSearchParams()` wrapped in Suspense boundary
    - `npx tsc --noEmit` passes with no errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no type errors
- `npx next build` — success page route compiles without error
- Manual: visit `/checkout/success` → cart clears, thank-you message visible, button links to `/dashboard/orders`, auto-redirect fires after 6s
</verification>

<success_criteria>
- Success page exists at `/checkout/success` and is reachable
- Cart is emptied on page load
- User sees branded thank-you with green check icon
- Navigation button to `/dashboard/orders` works
- Auto-redirect fires after 6 seconds
</success_criteria>

<output>
After completion, create `.planning/quick/260727-k36-create-checkout-success-page-with-post-p/260727-k36-01-SUMMARY.md`
</output>
