---
phase: quick-260803-j2w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/api.ts"
  - "src/app/[locale]/dashboard/reservations/page.tsx"
autonomous: true
requirements: [QT-260803-J2W]
user_setup: []

must_haves:
  truths:
    - "The Reservations dashboard page fetches the logged-in user's room reservations from GET /api/reservations/rooms/me (Bearer JWT via apiFetch, never a hand-rolled fetch)"
    - "Each reservation card shows the room name (from room.name), reservation id, check-in date, check-out date, total price in EUR, and a status badge"
    - "The status badge renders the three real API statuses correctly: CONFIRMED (green), PENDING (yellow), CANCELLED (red) — not the old Title-case mock values"
    - "While loading, skeleton cards mimicking the reservation card shape are shown"
    - "When the user has no reservations, an empty state with a 'Explore our rooms' link to /rooms is shown"
    - "When the fetch fails, an error message is shown (not a crash)"
  artifacts:
    - path: "src/lib/api.ts"
      provides: "RoomReservationResponse interface + fetchMyRoomReservations()"
      contains: "fetchMyRoomReservations"
    - path: "src/app/[locale]/dashboard/reservations/page.tsx"
      provides: "Client-side data fetching + dynamic rendering of the reservations grid"
      contains: "fetchMyRoomReservations"
  key_links:
    - from: "src/app/[locale]/dashboard/reservations/page.tsx"
      to: "src/lib/api.ts fetchMyRoomReservations"
      via: "useEffect + cancelled-guard (same pattern as dashboard/orders/page.tsx)"
      pattern: "fetchMyRoomReservations"
    - from: "page.tsx status badge"
      to: "res.status (uppercase enum)"
      via: "switch on 'CONFIRMED' | 'PENDING' | 'CANCELLED'"
      pattern: "CANCELLED"
    - from: "page.tsx empty state"
      to: "/rooms route"
      via: "next/link with 'Explore our rooms'"
      pattern: "href=\"/rooms\""
---

<objective>
Wire the User Reservations dashboard page to live backend data.

Purpose: The page currently renders two hardcoded mock cards with Title-case statuses ("Confirmed"/"Pending"). This quick replaces that mock with a real client-side fetch of `GET /api/reservations/rooms/me` (JWT-authenticated via the existing `apiFetch`), plus the standard loading skeleton / error / empty states used across the dashboard.

Output:
- `RoomReservationResponse` interface + `fetchMyRoomReservations()` added to `src/lib/api.ts`
- `src/app/[locale]/dashboard/reservations/page.tsx` rewritten as a client component rendering the live reservations grid with dynamic status badges
</objective>

<execution_context>
@/Users/gabrielesuardi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/gabrielesuardi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/[locale]/dashboard/orders/page.tsx
@src/app/[locale]/dashboard/page.tsx

# AGENTS.md: this is Next.js 16 with breaking changes. This page is a plain client
# component using no new Next APIs (no params, no useSearchParams) — it clones the
# working pattern in dashboard/orders/page.tsx. Consult
# node_modules/next/dist/docs/ only if something behaves unexpectedly.

# Backend contract (VERIFIED — do not re-research):
# GET /api/reservations/rooms/me -> 200, List<RoomReservationResponseDTO>, requires JWT.
# userId is derived server-side from the principal.
# Per-item JSON:
#   { id: UUID string,
#     room: { name: string, ...RoomResponse fields },
#     userId: string,
#     checkInDate: "YYYY-MM-DD",
#     checkOutDate: "YYYY-MM-DD",
#     totalPrice: number (BigDecimal -> number),
#     status: "CONFIRMED" | "PENDING" | "CANCELLED",
#     createdAt: ISO datetime, updatedAt: ISO datetime }

<interfaces>
From src/lib/api.ts (existing — executor uses directly, no exploration needed):

```typescript
const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8080";
export function getToken(): string | undefined;
async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>;
// apiFetch prepends API_BASE_URL, injects { Authorization: `Bearer ${token}` }
// via getAuthHeaders(), and on 401/403 removes the token + redirects to /login.

export interface Room {
  id: string;
  name: string;
  description: string;
  pricePerNight: number;
  amenities: string[];
  imageUrls: string[];
  maxCapacity?: number;
}
```

From src/app/[locale]/dashboard/orders/page.tsx (the canonical fetch pattern to clone):

```typescript
// "use client" + useState/useEffect with cancelled-guard:
//   const [orders, setOrders] = useState<Order[]>([]);
//   const [loading, setLoading] = useState(true);
//   const [error, setError] = useState<string | null>(null);
//   useEffect(() => { let cancelled = false; async function load() { ... } load();
//     return () => { cancelled = true; }; }, []);
// Badge helper: function statusBadgeClass(status: string): string { switch (...) }
// States rendered in order: loading -> error -> empty -> data
```

Conventions to honor (from the codebase, do not contradict):
- EUR formatting: `€{value.toFixed(2)}` — same as the rooms detail page
  (`src/app/[locale]/(vetrina)/rooms/[id]/page.tsx` uses `€{room.pricePerNight.toFixed(2)}`).
  Do NOT introduce Intl.NumberFormat — the app convention is the `€{n.toFixed(2)}` template.
- Dates: `checkInDate`/`checkOutDate` already arrive as "YYYY-MM-DD" strings — render them
  as-is (the cleaner option vs re-parsing with date-fns; date-fns stays unused on this page).
- Status badge styling follows the existing pill convention:
  `inline-block rounded-full px-2.5 py-0.5 text-xs font-medium` + status color classes.
- Auth: do NOT gate the fetch on useAuth. The /dashboard/* segment is route-protected
  (src/proxy.ts) and apiFetch self-redirects to /login on 401. The page fetches directly
  in useEffect, exactly like orders/page.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RoomReservationResponse interface and fetchMyRoomReservations to the API client</name>
  <files>src/lib/api.ts</files>
  <action>
    Add two exports to `src/lib/api.ts` (place next to the existing reservation code near `RoomReservationRequest`/`createRoomReservation`):

    1. `export interface RoomReservationResponse` with exactly these fields matching the verified backend DTO:
       - `id: string` (UUID)
       - `room: { id: string; name: string }` — the response embeds a full RoomResponse; the page only renders `room.name`. Type it as `{ id: string; name: string }` (structural typing accepts the richer backend payload). Do NOT widen to `Room` — keep the type minimal to what the page consumes.
       - `userId: string`
       - `checkInDate: string` // "YYYY-MM-DD"
       - `checkOutDate: string` // "YYYY-MM-DD"
       - `totalPrice: number` // BigDecimal serialized as JSON number
       - `status: "CONFIRMED" | "PENDING" | "CANCELLED"` — the literal union, NOT `string`; the page switch depends on it.
       - `createdAt: string` // ISO datetime
       - `updatedAt: string` // ISO datetime

    2. `export async function fetchMyRoomReservations(): Promise<RoomReservationResponse[]>` that returns `apiFetch<RoomReservationResponse[]>("/api/reservations/rooms/me")`.

    Do NOT create a new fetch helper or touch `apiFetch`/`getAuthHeaders` — the existing client already injects `Authorization: Bearer` (this satisfies the JWT requirement per codebase convention; the description's mention of raw `process.env.NEXT_PUBLIC_API_URL` is superseded by API_BASE_URL + apiFetch).
  </action>
  <verify>
    <automated>
      grep -n "fetchMyRoomReservations" src/lib/api.ts | grep -v "^#" | grep -c "apiFetch<RoomReservationResponse\[\]>"  # expect >= 1
      npx tsc --noEmit
    </automated>
  </verify>
  <done>
    `RoomReservationResponse` interface and `fetchMyRoomReservations()` exist as named exports in src/lib/api.ts, typecheck passes, and the function delegates to `apiFetch<RoomReservationResponse[]>("/api/reservations/rooms/me")`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the reservations page as a live client component with status badges, skeletons, and empty state</name>
  <files>src/app/[locale]/dashboard/reservations/page.tsx</files>
  <action>
    Replace the entire content of `src/app/[locale]/dashboard/reservations/page.tsx` (currently a server component with hardcoded mock cards) with a client component. Clone the proven pattern from `src/app/[locale]/dashboard/orders/page.tsx`:

    1. Add `"use client"` as the first line. Imports: `useState`, `useEffect` from react; `Link` from next/link; `fetchMyRoomReservations`, `type RoomReservationResponse` from `@/lib/api`; `Skeleton` from `@/components/ui/skeleton`; `Card, CardContent, CardHeader, CardTitle` from `@/components/ui/card` (keep the current card imports).

    2. State: `const [reservations, setReservations] = useState<RoomReservationResponse[]>([])`, `const [loading, setLoading] = useState(true)`, `const [error, setError] = useState<string | null>(null)`.

    3. `useEffect` with the cancelled-guard pattern exactly like orders/page.tsx: `let cancelled = false;` -> async `load()` calling `fetchMyRoomReservations()`, `setError("Unable to load reservations. Please try again later.")` on catch, `setLoading(false)` in finally, cleanup sets `cancelled = true`. Empty dependency array.

    4. Status badge helper switching on the REAL uppercase API values (the old mock's Title-case "Confirmed"/"Pending" is gone — do not match on it):
       ```ts
       function statusBadgeClass(status: RoomReservationResponse["status"]): string {
         switch (status) {
           case "CONFIRMED": return "bg-green-100 text-green-800";
           case "PENDING":   return "bg-yellow-100 text-yellow-800";
           case "CANCELLED": return "bg-red-100 text-red-700";
         }
       }
       ```
       Render the badge as `<span className={`inline-block rounded-full px-2.5 py-0.5 text-xs font-medium ${statusBadgeClass(res.status)}`}>{res.status}</span>` inside the CardHeader flex row (replace the old ternary that only knew green/yellow).

    5. Render states in this order:
       - **loading** (`{loading && ...}`): 3 skeleton cards mimicking the reservation card shape — each a `<Card>` (no header/content text) containing: a `<Skeleton className="h-5 w-40" />` (room name line), `<Skeleton className="mt-2 h-3 w-24" />` (id line), `<Skeleton className="h-5 w-16 rounded-full" />` (badge pill), and a `grid grid-cols-2 gap-4` with `<Skeleton className="h-4 w-20" />` ×3 (check-in / check-out / total lines). Keep the page heading + subtitle always visible above the states.
       - **error** (`{!loading && error && ...}`): centered `<p className="text-muted-foreground">{error}</p>` in a `mt-8 text-center` div (same as orders page).
       - **empty** (`{!loading && !error && reservations.length === 0 && ...}`): centered "No reservations yet." message plus a `<Link href="/rooms" className="mt-4 inline-block text-sm font-medium text-primary underline underline-offset-4 hover:text-primary/80">Explore our rooms</Link>` (mirror the orders empty-state Link styling, same text as the description).
       - **data** (`{!loading && !error && reservations.length > 0 && ...}`): map over `reservations` in the existing `mt-8 space-y-4` div. Each card (same visual structure as the current mock, but real data):
         - CardHeader: left column = `CardTitle className="font-heading text-lg"` showing `res.room.name` + `<p className="text-xs text-muted-foreground">{`RES-${res.id.substring(0, 8).toUpperCase()}`}</p>` (shortened UUID per the task description); right = the status badge span.
         - CardContent: `grid grid-cols-2 gap-4 text-sm` with three rows — Check-in → `res.checkInDate`, Check-out → `res.checkOutDate`, Total → `<p className="font-medium text-accent">€{res.totalPrice.toFixed(2)}</p>` (EUR convention from the rooms page; `totalPrice` arrives as a number).
       - Key the list on `res.id` (UUID, unique).

    6. Keep the existing page heading `<h1 className="font-heading text-2xl font-semibold">Reservations</h1>` and the subtitle paragraph — only the card content becomes dynamic.

    Do NOT add auth-gating logic (dashboard is already route-protected; apiFetch redirects on 401), do NOT introduce date-fns (dates render as-is), do NOT touch other dashboard pages or components.
  </action>
  <verify>
    <automated>
      grep -n '"use client"' "src/app/[locale]/dashboard/reservations/page.tsx" | grep -c '"use client"'  # expect >= 1
      grep -v "^#" "src/app/[locale]/dashboard/reservations/page.tsx" | grep -c "fetchMyRoomReservations"  # expect >= 1
      grep -v "^#" "src/app/[locale]/dashboard/reservations/page.tsx" | grep -c "CANCELLED"  # expect >= 1 (uppercase switch present)
      grep -v "^#" "src/app/[locale]/dashboard/reservations/page.tsx" | grep -c 'href="/rooms"'  # expect >= 1 (empty-state link)
      npx tsc --noEmit
      npm run build
    </automated>
  </verify>
  <done>
    The page is a `"use client"` component that fetches `GET /api/reservations/rooms/me` via `fetchMyRoomReservations`, renders reservation cards from live data (room.name, id, checkInDate/checkOutDate as-is, `€{totalPrice.toFixed(2)}`), switches status badges on uppercase CONFIRMED/PENDING/CANCELLED, shows 3 skeleton cards while loading, an error message on failure, and an empty state linking "Explore our rooms" to /rooms. `tsc --noEmit` and `next build` both pass — the page no longer contains the hardcoded `reservations` mock array.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend API → client page | untrusted network payload crosses into the component; shapes the state that drives badge colors and price rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-j2w-01 | Tampering | GET /api/reservations/rooms/me payload | accept | Authenticated endpoint scoped to the principal server-side; page renders text only, no HTML injection vectors (React escapes), no links from API data |
| T-j2w-02 | Spoofing | JWT presentation | mitigate | Delegated to existing `apiFetch`/`getAuthHeaders` — Authorization Bearer injected from the `accessToken` cookie; 401/403 auto-redirects to /login |
| T-j2w-03 | DoS | Huge reservation list rendering | accept | Room reservations are bounded by real bookings; no pagination contract exists in the backend DTO — out of scope for this quick |
| T-j2w-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — date-fns already present and deliberately unused; zero supply-chain surface added |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (both tasks)
- `npm run build` passes (Next 16 production build with the page now a client component)
- Grep gates: `fetchMyRoomReservations` present in both api.ts and page.tsx; uppercase `CANCELLED` switch present; `href="/rooms"` empty-state link present
- Manual smoke (optional, requires running backend): visit /dashboard/reservations authenticated — cards render from API data, badges show CONFIRMED/PENDING/CANCELLED, skeleton shows during load
</verification>

<success_criteria>
- Reservations dashboard page renders the logged-in user's real reservations from `GET /api/reservations/rooms/me` via `apiFetch` (Bearer JWT injected by the existing client — no hand-rolled fetch, no raw `process.env.NEXT_PUBLIC_API_URL` usage)
- Status badges switch on the real uppercase API values with three distinct colors
- Loading skeletons, error message, and empty state ("Explore our rooms" → /rooms) all implemented
- EUR total rendered as `€{totalPrice.toFixed(2)}` per app convention; dates rendered as-is (already YYYY-MM-DD)
- Zero new dependencies; tsc and next build pass
</success_criteria>

<output>
Create `.planning/quick/260803-j2w-implement-the-data-fetching-and-dynamic-/260803-j2w-SUMMARY.md` when done
</output>
