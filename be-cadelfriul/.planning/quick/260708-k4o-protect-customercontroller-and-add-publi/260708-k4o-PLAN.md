# Quick Task 260708-k4o: Protect CustomerController and add public registration endpoint

## Tasks

1. Add `POST /api/auth/customer/register` in AuthenticationController
2. Remove `POST /api/customers` and add `@PreAuthorize` to CustomerController
3. Update SecurityConfig with `@EnableMethodSecurity` and role-based URL security
