---
phase: 260721-o5n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/cadelfriul/backend/hospitality/entity/Room.java
  - src/main/java/com/cadelfriul/backend/hospitality/entity/RoomType.java
  - src/main/java/com/cadelfriul/backend/hospitality/repository/RoomRepository.java
  - src/main/java/com/cadelfriul/backend/hospitality/dto/RoomRequest.java
  - src/main/java/com/cadelfriul/backend/hospitality/dto/RoomResponse.java
  - src/main/java/com/cadelfriul/backend/hospitality/service/AdminRoomService.java
  - src/main/java/com/cadelfriul/backend/hospitality/service/PublicRoomService.java
  - src/main/java/com/cadelfriul/backend/hospitality/controller/AdminRoomController.java
  - src/main/java/com/cadelfriul/backend/hospitality/controller/PublicRoomController.java
  - src/main/java/com/cadelfriul/backend/core/config/SecurityConfig.java
autonomous: true
requirements: []

must_haves:
  truths:
    - "Admin can create, update, and delete rooms via /api/admin/rooms"
    - "Public users can list available rooms and get a room by ID via /api/rooms"
    - "Room data persists with all fields (name, description, pricePerNight, capacity, roomType, roomNumber, floor, isAvailable)"
    - "Public room endpoints are accessible without authentication"
    - "Admin room endpoints require SUPER_ADMIN role"
  artifacts:
    - path: "src/main/java/com/cadelfriul/backend/hospitality/entity/Room.java"
      provides: "Room JPA entity"
      contains: "@Entity"
    - path: "src/main/java/com/cadelfriul/backend/hospitality/entity/RoomType.java"
      provides: "Room type enum"
      contains: "enum RoomType"
    - path: "src/main/java/com/cadelfriul/backend/hospitality/repository/RoomRepository.java"
      provides: "Room Spring Data repository"
      contains: "JpaRepository"
    - path: "src/main/java/com/cadelfriul/backend/hospitality/dto/RoomRequest.java"
      provides: "Room create/update DTO"
    - path: "src/main/java/com/cadelfriul/backend/hospitality/dto/RoomResponse.java"
      provides: "Room response DTO"
    - path: "src/main/java/com/cadelfriul/backend/hospitality/service/AdminRoomService.java"
      provides: "Admin room CRUD service"
      contains: "@Service"
    - path: "src/main/java/com/cadelfriul/backend/hospitality/service/PublicRoomService.java"
      provides: "Public room read service"
      contains: "@Service"
    - path: "src/main/java/com/cadelfriul/backend/hospitality/controller/AdminRoomController.java"
      provides: "Admin room REST endpoints"
      contains: "@RestController"
    - path: "src/main/java/com/cadelfriul/backend/hospitality/controller/PublicRoomController.java"
      provides: "Public room REST endpoints"
      contains: "@RestController"
    - path: "src/main/java/com/cadelfriul/backend/core/config/SecurityConfig.java"
      provides: "Security rules for /api/rooms/**"
      contains: "/api/rooms"
  key_links:
    - from: "src/main/java/com/cadelfriul/backend/hospitality/controller/AdminRoomController.java"
      to: "src/main/java/com/cadelfriul/backend/hospitality/service/AdminRoomService.java"
      via: "constructor injection"
    - from: "src/main/java/com/cadelfriul/backend/hospitality/controller/PublicRoomController.java"
      to: "src/main/java/com/cadelfriul/backend/hospitality/service/PublicRoomService.java"
      via: "constructor injection"
    - from: "src/main/java/com/cadelfriul/backend/core/config/SecurityConfig.java"
      to: "PublicRoomController"
      via: "/api/rooms/** → permitAll()"
---

<objective>
Implement the complete backend layer for the Room feature in the hospitality package: Entity, Repository, DTOs, Services, Controllers, and SecurityConfig update.

Purpose: Establish the Room domain as the foundational entity for the hospitality module, following the exact same patterns as the ecommerce module (Product).

Output: Full CRUD backend for Rooms — admin endpoints for management, public endpoints for browsing.
</objective>

<execution_context>
@/Users/gabrielesuardi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/gabrielesuardi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Patterns to follow — copy structure exactly from ecommerce module:

From src/main/java/com/cadelfriul/backend/ecommerce/entity/Product.java:
- @Entity @Table, @Id @GeneratedValue(UUID), manual getters/setters, no Lombok
- @ManyToOne LAZY for relationships, @ElementCollection for maps

From src/main/java/com/cadelfriul/backend/ecommerce/repository/ProductRepository.java:
- extends JpaRepository<Entity, UUID>, derived query methods, @Query for complex filters

From src/main/java/com/cadelfriul/backend/ecommerce/dto/ProductRequest.java:
- Mutable class, private fields, getters/setters, no annotations

From src/main/java/com/cadelfriul/backend/ecommerce/dto/ProductResponse.java:
- Immutable-style, private final fields, constructor from entity

From src/main/java/com/cadelfriul/backend/ecommerce/service/AdminProductService.java:
- @Service @Transactional, constructor injection, RuntimeException("Entity not found with id: " + id)

From src/main/java/com/cadelfriul/backend/ecommerce/service/PublicProductService.java:
- @Service @Transactional(readOnly = true), constructor injection

From src/main/java/com/cadelfriul/backend/ecommerce/controller/AdminProductController.java:
- @RestController @RequestMapping @PreAuthorize("hasRole('SUPER_ADMIN')") @Tag, constructor injection
- Returns ResponseEntity with CREATED, OK, NO_CONTENT

From src/main/java/com/cadelfriul/backend/ecommerce/controller/PublicProductController.java:
- @RestController @RequestMapping @Tag, NO security annotation

From src/main/java/com/cadelfriul/backend/core/config/SecurityConfig.java:
- /api/products/** → permitAll(), /api/admin/** → authenticated()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hospitality foundation — Room entity, RoomType enum, RoomRepository, DTOs</name>
  <files>
    src/main/java/com/cadelfriul/backend/hospitality/entity/RoomType.java,
    src/main/java/com/cadelfriul/backend/hospitality/entity/Room.java,
    src/main/java/com/cadelfriul/backend/hospitality/repository/RoomRepository.java,
    src/main/java/com/cadelfriul/backend/hospitality/dto/RoomRequest.java,
    src/main/java/com/cadelfriul/backend/hospitality/dto/RoomResponse.java
  </files>
  <action>
Create the entire hospitality package foundation. Follow Product.java patterns exactly (manual getters/setters, no Lombok, UUID primary key).

**RoomType enum** (`hospitality/entity/RoomType.java`):
- Values: SINGLE, DOUBLE, TRIPLE, QUADRUPLE, SUITE, FAMILY, DELUXE
- Package: `com.cadelfriul.backend.hospitality.entity`

**Room entity** (`hospitality/entity/Room.java`):
- `@Entity @Table(name = "rooms")`
- Fields:
  - `@Id @GeneratedValue(strategy = GenerationType.UUID) private UUID id;`
  - `@Column(nullable = false) private String name;` — room name (e.g., "Camera Panoramica")
  - `@Column(columnDefinition = "TEXT") private String description;`
  - `@Column(nullable = false, precision = 10, scale = 2) private BigDecimal pricePerNight;`
  - `@Column(nullable = false) private int capacity;` — max guests
  - `@Column(nullable = false) @Enumerated(EnumType.STRING) private RoomType roomType;`
  - `@Column(nullable = false) private String roomNumber;` — e.g., "101", "A-204"
  - `@Column(nullable = false) private int floor;`
  - `@Column(nullable = false) private boolean isAvailable = true;`
- No-arg constructor, manual getters/setters (match Product.java style exactly)

**RoomRepository** (`hospitality/repository/RoomRepository.java`):
- `extends JpaRepository<Room, UUID>`
- Methods: `List<Room> findByIsAvailableTrue();`
- Filtered query using @Query:
  ```
  SELECT r FROM Room r
  WHERE r.isAvailable = true
  AND (:roomType IS NULL OR r.roomType = :roomType)
  AND (:minCapacity IS NULL OR r.capacity >= :minCapacity)
  AND (:maxPrice IS NULL OR r.pricePerNight <= :maxPrice)
  AND (:search IS NULL OR LOWER(r.name) LIKE LOWER(CONCAT('%', :search, '%')) OR LOWER(r.description) LIKE LOWER(CONCAT('%', :search, '%')))
  ```

**RoomRequest DTO** (`hospitality/dto/RoomRequest.java`):
- Mutable class, private fields, getters/setters (match ProductRequest.java pattern)
- Fields: name, description, pricePerNight (BigDecimal), capacity (int), roomType (RoomType), roomNumber (String), floor (int), isAvailable (boolean)

**RoomResponse DTO** (`hospitality/dto/RoomResponse.java`):
- Immutable-style with private final fields (match ProductResponse.java pattern)
- Constructor: `RoomResponse(Room room)` — extracts all fields from entity
- Getters for all fields
- Fields: id (UUID), name, description, pricePerNight, capacity, roomType, roomNumber, floor, isAvailable
  </action>
  <verify>
    <automated>find src/main/java/com/cadelfriul/backend/hospitality -name "*.java" | wc -l | grep -q "5" && echo "PASS: 5 files created" || echo "FAIL"</automated>
  </verify>
  <done>5 Java files exist in hospitality package. Room entity has all fields with getters/setters. RoomType enum has all values. Repository extends JpaRepository with filtered query. Both DTOs follow Product patterns exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Create AdminRoomService + PublicRoomService + AdminRoomController + PublicRoomController</name>
  <files>
    src/main/java/com/cadelfriul/backend/hospitality/service/AdminRoomService.java,
    src/main/java/com/cadelfriul/backend/hospitality/service/PublicRoomService.java,
    src/main/java/com/cadelfriul/backend/hospitality/controller/AdminRoomController.java,
    src/main/java/com/cadelfriul/backend/hospitality/controller/PublicRoomController.java
  </files>
  <action>
Create services and controllers following the Product module patterns exactly.

**AdminRoomService** (`hospitality/service/AdminRoomService.java`):
- `@Service @Transactional`
- Constructor injection of `RoomRepository`
- Methods:
  - `createRoom(RoomRequest request)` → creates Room from request fields, saves, returns `RoomResponse`
  - `updateRoom(UUID id, RoomRequest request)` → finds by id (RuntimeException "Room not found with id: " + id), applies non-null fields from request, saves, returns `RoomResponse`
  - `deleteRoom(UUID id)` → checks existsById (RuntimeException if missing), deletes
- Private helper `applyRoomRequest(Room room, RoomRequest request)` — applies non-null fields (match `applyProductRequest` pattern)
- Private helper `toResponse(Room room)` → `new RoomResponse(room)`

**PublicRoomService** (`hospitality/service/PublicRoomService.java`):
- `@Service @Transactional(readOnly = true)`
- Constructor injection of `RoomRepository`
- Methods:
  - `getAvailableRooms(RoomType roomType, Integer minCapacity, BigDecimal maxPrice, String search)` → calls `roomRepository` filtered query, maps to `RoomResponse` list. Use safe null handling for search (match PublicProductService pattern: if search is null, pass null to query which handles it via `(:search IS NULL OR ...)`)
  - `getRoomById(UUID id)` → finds by id (RuntimeException "Room not found with id: " + id), returns `RoomResponse`

**AdminRoomController** (`hospitality/controller/AdminRoomController.java`):
- `@RestController @RequestMapping("/api/admin/rooms")`
- `@PreAuthorize("hasRole('SUPER_ADMIN')")`
- `@Tag(name = "Admin Rooms", description = "Admin endpoints for managing rooms")`
- Constructor injection of `AdminRoomService`
- Endpoints:
  - `POST /` → `@Operation(summary = "Create room")`, returns `ResponseEntity.status(HttpStatus.CREATED).body(service.createRoom(request))`
  - `PUT /{id}` → `@Operation(summary = "Update room")`, returns `ResponseEntity.ok(service.updateRoom(id, request))`
  - `DELETE /{id}` → `@Operation(summary = "Delete room")`, returns `ResponseEntity.noContent().build()`

**PublicRoomController** (`hospitality/controller/PublicRoomController.java`):
- `@RestController @RequestMapping("/api/rooms")`
- NO security annotation (public endpoint, permitted via SecurityConfig)
- `@Tag(name = "Public Rooms", description = "Public endpoints for browsing available rooms")`
- Constructor injection of `PublicRoomService`
- Endpoints:
  - `GET /` → `@Operation(summary = "List available rooms")`, accepts `@RequestParam(required = false)` for roomType, minCapacity, maxPrice, search. Returns `ResponseEntity.ok(service.getAvailableRooms(...))`
  - `GET /{id}` → `@Operation(summary = "Get room by ID")`, returns `ResponseEntity.ok(service.getRoomById(id))`
  </action>
  <verify>
    <automated>find src/main/java/com/cadelfriul/backend/hospitality -name "*.java" | wc -l | grep -q "9" && echo "PASS: 9 files total" || echo "FAIL"</automated>
  </verify>
  <done>9 Java files exist in hospitality package. AdminRoomService has CRUD methods. PublicRoomService has read-only methods. AdminRoomController has POST/PUT/DELETE with @PreAuthorize. PublicRoomController has GET endpoints with no security. All follow Product module patterns.</done>
</task>

<task type="auto">
  <name>Task 3: Update SecurityConfig to permit public room endpoints</name>
  <files>
    src/main/java/com/cadelfriul/backend/core/config/SecurityConfig.java
  </files>
  <action>
Update SecurityConfig.java to add the public rooms endpoint to the permitAll list.

Add this line in the `authorizeHttpRequests` block, right after the `/api/products/**` permitAll line (around line 48):

```java
.requestMatchers("/api/rooms/**").permitAll()
```

The admin endpoints `/api/admin/rooms/**` are already covered by the existing `.anyRequest().authenticated()` rule, so no additional admin rule is needed — the `@PreAuthorize("hasRole('SUPER_ADMIN')")` on AdminRoomController handles role enforcement.

Do NOT change anything else in SecurityConfig.java.
  </action>
  <verify>
    <automated>grep -c "/api/rooms" src/main/java/com/cadelfriul/backend/core/config/SecurityConfig.java | grep -q "1" && echo "PASS: /api/rooms added to SecurityConfig" || echo "FAIL"</automated>
  </verify>
  <done>SecurityConfig.java has `/api/rooms/**` → permitAll(). No other lines changed. Existing product and admin rules untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→PublicRoomController | Unauthenticated users can read room data (intentional — public catalog) |
| client→AdminRoomController | Authenticated + SUPER_ADMIN role required for room mutations |
| AdminRoomController→AdminRoomService | Input validation boundary — request fields applied to entity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260721-01 | Tampering | AdminRoomController | mitigate | @PreAuthorize("hasRole('SUPER_ADMIN')") on controller class enforces role check at method entry |
| T-260721-02 | Information Disclosure | PublicRoomController | accept | Room data is public catalog — no sensitive fields exposed |
| T-260721-03 | Elevation of Privilege | AdminRoomService | mitigate | RuntimeException on missing entity prevents orphaned operations; Spring Security filter chain before controller |
</threat_model>

<verification>
1. All 9 Java files exist under `src/main/java/com/cadelfriul/backend/hospitality/`
2. SecurityConfig contains `/api/rooms/**` permitAll line
3. AdminRoomController has `@PreAuthorize("hasRole('SUPER_ADMIN')")`
4. PublicRoomController has NO security annotations
5. `mvn compile` or `./gradlew compileJava` passes (if build tool present)
</verification>

<success_criteria>
- Room entity persists with all fields (name, description, pricePerNight, capacity, roomType, roomNumber, floor, isAvailable)
- Admin CRUD: POST /api/admin/rooms, PUT /api/admin/rooms/{id}, DELETE /api/admin/rooms/{id}
- Public read: GET /api/rooms, GET /api/rooms/{id} with filters (roomType, minCapacity, maxPrice, search)
- All endpoints follow Product module conventions exactly
- SecurityConfig updated — public rooms accessible without auth, admin rooms require SUPER_ADMIN
</success_criteria>

<output>
After completion, create `.planning/quick/260721-o5n-implement-the-backend-logic-for-the-room/260721-o5n-SUMMARY.md`
</output>
