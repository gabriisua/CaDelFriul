---
phase: 260731-n1j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts
  - .planning/STATE.md
autonomous: true
requirements: [N1J-01, N1J-02, N1J-03]

must_haves:
  truths:
    - "Edit dialog renders thumbnail previews of the product's existing images just above the file input"
    - "Each thumbnail is wrapped in a relative container with an absolute-positioned X delete button"
    - "Clicking X removes the thumbnail from the previews (visual feedback)"
    - "Partial image URLs are mapped to full URLs via environment.apiUrl before rendering"
  artifacts:
    - path: "src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts"
      provides: "existingImageUrls state, Edit-mode URL mapping, preview template, removeExistingImage"
      contains: "existingImageUrls: string[] = []"
    - path: "src/app/core/models/product.model.ts"
      provides: "imageUrls field on Product"
      contains: "imageUrls: string[]"
  key_links:
    - from: "ngOnChanges (Edit mode)"
      to: "environment.apiUrl"
      via: "partial URL → full URL mapping"
      pattern: "environment\\.apiUrl"
    - from: "template @for loop"
      to: "removeExistingImage"
      via: "X button (click) binding"
      pattern: "removeExistingImage\\(i\\)"
    - from: "@if (existingImageUrls.length > 0)"
      to: "input type=file"
      via: "preview block placed directly above file input"
      pattern: "existingImageUrls\\.length > 0"
---

<objective>
Verify the existing working-tree implementation of the Angular "Edit Product" image previews satisfies ALL requirements, fix any gaps found, and commit the remaining uncommitted template block atomically.

Purpose: This task continues quick task 260731-my3. Commit 142c236 already landed the class logic (`existingImageUrls` property, Edit-mode URL mapping, `removeExistingImage` splice) and the `imageUrls` field on Product. The working tree carries 15 uncommitted insertions adding the template preview block (verified present via `git diff`). This plan confirms requirement coverage with grep gates + a production build, then commits the delta.

Output: Committed component with existing-image previews, STATE.md row added.
</objective>

<execution_context>
@/Users/gabrielesuardi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/gabrielesuardi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts
@src/app/core/models/product.model.ts
@src/environments/environment.ts

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration needed. -->

From src/environments/environment.ts:
```typescript
export const environment = {
  production: true,
  apiUrl: 'http://localhost:8080',
};
```

From src/app/core/models/product.model.ts (line 12):
```typescript
imageUrls: string[];
```

From src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts (current working-tree state, line numbers as of this plan):
- Line 108: `existingImageUrls: string[] = [];`
- Lines 122-124 (ngOnChanges, Edit mode):
  ```typescript
  this.existingImageUrls = this.product.imageUrls?.map(url =>
    url.startsWith('http') ? url : `${environment.apiUrl}${url}`
  ) ?? [];
  ```
- Lines 149-151:
  ```typescript
  removeExistingImage(index: number): void {
    this.existingImageUrls.splice(index, 1);
  }
  ```
- Template (uncommitted, lines 54-68): `@if (existingImageUrls.length > 0)` → `<div class="flex gap-3 mt-2 flex-wrap">` → `@for (url of existingImageUrls; track url; let i = $index)` → `<div class="relative">` → `<img [src]="url" class="w-20 h-20 object-cover rounded-md shadow-sm border border-gray-200" />` → absolute X button `(click)="removeExistingImage(i)"` with `[attr.aria-label]="'Remove image ' + (i + 1)"`. File input is on line 76, directly below.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify requirement coverage, fix gaps if found, commit atomically</name>
  <files>src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts, .planning/STATE.md</files>
  <action>
    The implementation is expected to be complete. Do NOT rewrite working code. Run the verification gates below; only edit the file if a gate fails.

    Phase A — Requirement coverage gates (all must pass against src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts):

    N1J-01 (component class logic):
    - G1: property exists — grep `existingImageUrls: string\[\] = \[\]`
    - G2: Edit-mode mapping exists — grep `environment\.apiUrl` AND `imageUrls\?\.map` in the same file
    - G3: null/empty handling — mapping uses `?? \[\]` (empty imageUrls → empty previews, `@if` hides block)
    - G4: mock removal method — grep `removeExistingImage\(index: number\)` with body containing `.splice(index, 1)`

    N1J-02 (template logic):
    - G5: preview container is placed ABOVE the file input — container line number (< `input type="file"` line) AND has `flex gap-3 mt-2 flex-wrap` AND iterates with `@for (url of existingImageUrls; track url; let i = $index)`
    - G6: img renders per URL with premium thumbnail classes `w-20 h-20 object-cover rounded-md shadow-sm border border-gray-200`

    N1J-03 (UI/UX bonus):
    - G7: each img wrapped in `<div class="relative">` with absolute X button (`absolute` + `(click)="removeExistingImage(i)"` + `aria-label`)

    If ANY gate fails: fix the discrepancy minimally in the .ts file (inline template), preserving the existing committed class logic untouched. If all gates pass, make NO code edits.

    Phase B — Compile verification:
    Run `npm run build` from the repo root (bo-cadelfriul). The Angular compiler validates both the TS class logic and the inline template syntax. If the build fails on a template/TS error introduced by the uncommitted block, fix it.

    Phase C — Commit:
    After all gates pass and build succeeds, commit atomically:
    1. Add the STATE.md row (see below).
    2. Stage ONLY `src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts` and `.planning/STATE.md` (verify with `git status --short` that no other files are staged).
    3. Commit message per repo convention: `feat(quick-260731-n1j): add existing image previews to product edit dialog` (mirrors prior `feat(quick-260731-my3): ...` style).

    STATE.md update (bo-cadelfriul/.planning/STATE.md): insert a new row in the "Quick Tasks Completed" table above the "Last activity" line:
    `| 260731-n1j | Update the Angular Edit Product component to display visual previews of existing product images | 2026-07-31 | {new commit hash} | [260731-n1j-update-the-angular-edit-product-componen](./quick/260731-n1j-update-the-angular-edit-product-componen/) |`
    Update "Last activity" line to reference 260731-n1j. Use the actual commit hash once created.
  </action>
  <verify>
    <automated>
      # Gates (from repo root bo-cadelfriul)
      grep -c "existingImageUrls: string\[\] = \[\]" src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts
      grep -c "environment\\.apiUrl" src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts
      grep -c "imageUrls?.map" src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts
      grep -c "?? \[\]" src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts
      grep -c "removeExistingImage" src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts
      grep -c "flex gap-3 mt-2 flex-wrap" src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts
      grep -c "w-20 h-20 object-cover rounded-md shadow-sm border border-gray-200" src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts
      grep -c "class=\"relative\"" src/app/features/products/components/product-edit-dialog/product-edit-dialog.component.ts
      # Build
      npm run build
      # Clean tree check
      git status --porcelain -- src/app/features/products/components/product-edit-dialog/ .planning/STATE.md
    </automated>
  </verify>
  <done>
    All 9 grep gates return count ≥ 1; `npm run build` exits 0; `git status` shows no uncommitted changes to the component or STATE.md; commit exists with the component + STATE.md only; STATE.md contains the 260731-n1j row with the real commit hash.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend API → template | `imageUrls` values from the Spring Boot API are rendered into `<img [src]>` bindings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260731-n1j-01 | Tampering | `img [src]="url"` binding | accept | Angular's built-in DOM sanitizer rejects non-sanitized schemes (e.g. `javascript:`) in `[src]`; no `DomSanitizer.bypassSecurityTrustUrl` is used. Mapping via `environment.apiUrl` prefix for relative paths only. |
| T-260731-n1j-02 | Information Disclosure | environment.apiUrl concat | accept | No PII; API base URL is a dev-localhost config already exposed in the served bundle. |
| T-260731-n1j-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — `npm run build` uses existing node_modules. |
</threat_model>

<verification>
- `npm run build` succeeds (Angular compiler validates inline template + TS).
- Grep gates G1–G7 all pass (count ≥ 1 each).
- `git status --porcelain` clean for the two modified files after commit.
- STATE.md updated with 260731-n1j row.
</verification>

<success_criteria>
- The product Edit dialog displays existing-image thumbnails above the file input with `gap-3` flex layout.
- Each thumbnail has an absolute-positioned X delete button that removes it from the preview list (visual feedback via `removeExistingImage` splice).
- Partial URLs are mapped to full URLs through `environment.apiUrl`; missing/empty `imageUrls` renders no preview block.
- All changes (component template block + STATE.md) committed atomically in one commit.
</success_criteria>

<output>
Create `.planning/quick/260731-n1j-update-the-angular-edit-product-componen/260731-n1j-SUMMARY.md` when done
</output>
