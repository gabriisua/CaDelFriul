# Quick Task 260716-el6: Align Staff model/UI to backend Admin entity

## Goal
Replace firstName+lastName with fullName, remove phone/active fields, fix grid "undefined undefined" bug.

## Tasks

### Task 1: Update Staff model
- **File:** `src/app/core/models/staff.model.ts`
- **Action:** Replace `firstName`, `lastName`, `phone`, `active`, `createdAt`, `updatedAt` with `fullName: string`. Update `StaffRequest` and `StaffRole` enum.
- **Verify:** Build passes

### Task 2: Fix Staff component grid
- **File:** `src/app/features/staff/staff.component.ts`
- **Action:** Remove `fullName` concatenation in `loadStaff()`, fix confirm dialog message and logs title to use `fullName`
- **Verify:** Build passes

### Task 3: Fix Staff edit dialog
- **File:** `src/app/features/staff/components/staff-edit-dialog/staff-edit-dialog.component.ts`
- **Action:** Replace firstName/lastName controls with fullName, remove phone/active, update form patching
- **Verify:** Build passes
